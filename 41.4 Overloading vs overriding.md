2025-10-26 20:17
Tags: #toCheck 

### **41.4.1. Основные понятия**

- Определение _overloading_ (перегрузка методов)
    
- Определение _overriding_ (переопределение методов)
    
- Сравнение целей: compile-time vs runtime polymorphism
    
- Требования языка для каждого механизма
    

### **41.4.2. Method Overloading (Перегрузка методов)**

- Правила перегрузки: различие по сигнатуре
    
- Возвращаемый тип **не влияет** на перегрузку
    
- Varargs, generics, boxing/unboxing при перегрузке
    
- Best practices: избегать неоднозначности
    
- Примеры компиляторных конфликтов
    

### **41.4.3. Method Overriding (Переопределение)**

- Контракт переопределения: сигнатура, совместимый возвращаемый тип (covariant return)
    
- Правила доступа и исключений
    
- Аннотация `@Override` — зачем обязательна в продакшн-коде
    
- Runtime dispatch (late binding)
    
- Final, static и private методы — **не могут быть overridden**
    
- Abstract и default methods — где используются
    

---

## 41.4.4. Методы из класса `java.lang.Object`

### **41.4.4.1. equals(Object)**

- Почему equals — это классический пример overriding
    
- Контракт equals (рефлексивность, симметричность, транзитивность, непротиворечивость, non-null)
    
- Примеры правильной и неправильной реализации
    

### **41.4.4.2. hashCode()**

- Обязательство переопределять вместе с equals
    
- Последствия нарушения (HashMap, HashSet некорректная работа)
    
- Пример нарушения контракта
    

### **41.4.4.3. toString()**

- Переопределение для логирования, отладки
    
- Рекомендации (JSON-подобный формат в records, data-классах)
    

### **41.4.4.4. finalize()/Cleaner и clone()**

- clone() как пример защищённого метода с контрактом переопределения
    
- finalize() — устаревший пример, связь с overriding
    

---

## **41.4.5. Covariant Return Types (ковариантные возвращаемые типы)**

- Что это такое
    
- Почему разрешено при overriding, но не при overloading
    
- Примеры: `clone()` → `Object` vs `MyClass`
    

---

## **41.4.6. @Override аннотация**

- Как помогает выявить ошибки
    
- Обязательность при переопределении интерфейсных методов в современных стандартах
    
- Отличия между Java 5, 6 и 8+ по использованию `@Override`
    

---

## **41.4.7. Static методы: Hiding vs Overriding**

- Static methods не переопределяются (overriding запрещён)
    
- Понятие “Method Hiding”
    
- Таблица различий
    

---

## **41.4.8. Overloading Resolution: порядок выбора метода компилятором**

- Примитивы vs обёртки
    
- Varargs
    
- Generics
    
- Ambiguous method calls (ошибка компиляции)
    

---

## **41.4.9. Типичные ошибки и ловушки**

- Перегрузка вместо переопределения из-за ошибки сигнатуры
    
- Нарушение equals/hashCode
    
- Перегрузка с примитивами и autoboxing → неожиданный результат
    

---

## **41.4.10. Сводная таблица сравнения Overloading vs Overriding**

|Признак|Overloading|Overriding|
|---|---|---|
|Механизм|Compile-time decision|Runtime dispatch|
|Наследование|Не требуется|Требуется|
|Сигнатура|Меняется|Должна совпадать|
|Возвращаемый тип|Может отличаться|Ковариантный|
|Аннотация @Override|Не используется|Обязательна|

---

💡 **Замечание:**  
Контракт `equals()/hashCode()` вводится здесь как _пример overriding_, но **не расшифровывается полностью** — полное описание будет в специальном справочном разделе: **"Object Contract"**, на который здесь даётся ссылка:

> _“Подробный контракт методов equals(), hashCode(), toString() см. §41.X: The Object Contract”_

---

### ✅ Готов продолжить и написать **подраздел 41.4.1** или **41.4.4** в полном виде.

С какого начать?