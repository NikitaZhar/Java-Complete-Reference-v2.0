2025-11-04 13:11
Tags: #


- **`java.util.Arrays`**: `equals/deepEquals`, `hashCode/deepHashCode`, `toString/deepToString`, `compare/mismatch`, `fill`, `setAll/parallelSetAll`, `copyOf/Range`, `sort/parallelSort`, `binarySearch`, `stream`.
    
- **Массивы ↔ коллекции/стримы**: `Arrays.asList`, `List.of` vs массивы; `Stream.of`, примитивные стримы (`IntStream.of`, `Arrays.stream(int[])`), `toArray(IntFunction<T[]>)`.
    
- **Varargs**: это синтаксис поверх массивов; defensive copy входного varargs; ловушки перегрузок.
    
- **Reflection**: `java.lang.reflect.Array` — создание/доступ/длина; когда оправдано.

- Концентрировано про `java.util.Arrays` (`equals/deepEquals/hashCode/deepHashCode/toString/deepToString/compare/mismatch/fill/setAll/parallelSetAll/stream`).
    
- Конверсии массив↔стрим/коллекции (`Arrays.asList`, `Stream.of`, `IntStream.of`, `toArray`).
    
- Varargs как синтаксис поверх массивов (defensive copy).
    
- `java.lang.reflect.Array` — резюме (создание/доступ/длина).
    