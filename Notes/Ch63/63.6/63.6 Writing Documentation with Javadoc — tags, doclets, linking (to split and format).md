2025-10-28 18:54
Tags: #toCheck #toFormat 


# **63.6 Writing Documentation with Javadoc — tags, doclets, linking**


## **63.6.0 Purpose and Scope — Роль документации Javadoc в промышленной Java**

### **63.6.0.1 Что такое Javadoc**

**Javadoc** — это стандартная система документирования исходного кода Java, которая позволяет создавать структурированную, машиночитаемую и человекочитаемую спецификацию API, автоматически извлекаемую из комментариев формата `/** ... */`.

**Javadoc — это не комментарий к коду. Это часть API.**

### **63.6.0.2 Для чего Javadoc используется в промышленности**

- Формальная спецификация поведения классов, методов, интерфейсов и модулей.
    
- Договор (контракт) между разработчиком библиотеки и пользователем API.
    
- Автоматическая генерация HTML/PDF документации.
    
- Использование IDE (IntelliJ IDEA, Eclipse) для вывода подсказок.
    
- Инструмент контроля качества кода (на CI, при code review).
    

> Javadoc документирует _поведение и гарантии_, а не реализацию.

---

## **63.6.1 Javadoc Comments vs Inline Comments**

|Характеристика|Inline комментарии (`//`, `/* */`)|Javadoc (`/** */`)|
|---|---|---|
|Назначение|Объяснение реализации|Документирование API|
|Обрабатывается javadoc|Нет|Да|
|Видно пользователю API|Нет|Да (через IDE, сайт, документацию)|
|Обязательность|Не обязательно|Обязательно для public API|

**Правило:**

- _Всё, что является частью API (public, protected), должно быть документировано через Javadoc._
    
- _Всё, что является внутренней реализацией, может использовать обычные комментарии._
    

✅ Хороший пример:

```java
/**
 * Возвращает сумму двух целых чисел.
 *
 * @param a первое слагаемое
 * @param b второе слагаемое
 * @return сумма a и b
 * @implNote Этот метод не проверяет переполнение.
 */
public int sum(int a, int b) {
    return a + b;
}
```

---

## **63.6.2 Syntax of Javadoc Comments**

Формат Javadoc-комментария:

```java
/**
 * <описание метода или класса>
 *
 * <дополнительные абзацы, пояснения, примеры>
 *
 * @tag описание
 * @tag аргументы
 */
```

### **63.6.2.1 Основные правила**

- Первый абзац — _краткое описание_ (одной фразой).
    
- Далее можно давать детали: ограничения, побочные эффекты, особенности.
    
- После описания идут **теги**.
    

### **63.6.2.2 Поддерживаемые элементы внутри Javadoc**

|Элемент|Назначение|
|---|---|
|HTML|Разметка текста, таблицы, ссылки, списки|
|Markdown (Java 22+)|Поддерживается частично (`*текст*`, `**жирный**`)|
|Inline code|`{@code example}`|
|Preformatted code|`<pre><code>` или `{@snippet}`|

✅ Пример:

```java
/**
 * Выполняет копирование файла.
 *
 * <p>Если файл уже существует, он будет перезаписан.</p>
 *
 * <pre>{@code
 * FileCopier copier = new FileCopier();
 * copier.copy("a.txt", "b.txt");
 * }</pre>
 *
 * @param source путь к исходному файлу
 * @param target путь к целевому файлу
 * @throws IOException если чтение или запись невозможны
 */
```

---

## **63.6.3 Standard Tags — Основные теги Javadoc**

Стандартные теги должны идти **в фиксированном порядке**, что повышает читаемость и совместимость.

### **63.6.3.1 Обязательные теги**

|Тег|Назначение|Пример|Примечание|
|---|---|---|---|
|`@param`|описание параметров|`@param count количество элементов`|один тег на каждый параметр|
|`@return`|описание возвращаемого значения|`@return сумма элементов`|отсутствует, если метод `void`|
|`@throws` / `@exception`|описание исключений|`@throws IOException если файл не найден`|используется для checked исключений|

✅ Пример с контрактом:

```java
/**
 * Делит одно число на другое.
 *
 * @param dividend делимое
 * @param divisor делитель
 * @return результат деления
 * @throws ArithmeticException если divisor == 0
 */
public int divide(int dividend, int divisor) {
    return dividend / divisor;
}
```

---

### **63.6.3.2 API контракты и версии**

|Тег|Назначение|Где используется|
|---|---|---|
|`@since`|версия, где появился API|публичные библиотеки|
|`@deprecated`|помечает API устаревшим|обязательно пояснить альтернативу|
|`@apiNote`|рекомендация по API|публичный API|
|`@implSpec`|обязательство реализации|интерфейсы, abstract классы|
|`@implNote`|детали реализации|не влияет на контракт|

✅ Пример:

```java
/**
 * @deprecated Use {@link #newApi()} instead.
 */
@Deprecated(since="2.0", forRemoval=true)
public void oldApi() { }
```

---

### **63.6.3.3 Рекомендуемый порядок тегов**

```
@param
@return
@throws
@since
@deprecated
@see
@apiNote
@implSpec
@implNote
```

---

## **63.6.4 Inline Tags and Linking**

Инлайновые теги используются **внутри текста**, а не в конце.

|Тег|Пример|Назначение|
|---|---|---|
|`{@link ClassName#method}`|`{@link List#add(Object)}`|интерактивная ссылка|
|`{@linkplain ...}`|`{@linkplain Set множество}`|как link, но без форматирования шрифта|
|`{@code ...}`|`{@code a + b}`|моноширинный формат|
|`{@literal ...}`|`{@literal <tag>}`|отобразить без HTML-интерпретации|
|`{@value}`|отображение значения константы|только для `static final` полей|

✅ Пример:

```java
/**
 * См. {@link java.nio.file.Files#readAllBytes(Path)} для примера.
 * @apiNote Этот метод использует {@code InputStream} и не кэширует данные.
 */
```



## **63.6.5 Code Examples, Snippets, and Configuration**

Примеры кода в Javadoc должны быть **выполнимыми**, **точными** и **соответствовать лучшим практикам**. Java SE 18+ поддерживает _`@snippet` API_.

### **63.6.5.1 Использование блоков `@code` и `@snippet`**

#### ✅ Старый способ (совместим с Java 8–17):

```java
/**
 * <pre>{@code
 * List<String> list = new ArrayList<>();
 * list.add("A");
 * }</pre>
 */
```

#### ✅ Новый способ (Java 18+):

```java
/**
 * @snippet :
 * List<String> list = new ArrayList<>();
 * list.add("A");
 * System.out.println(list);
 */
```

### **63.6.5.2 Вставка меток (region markers)**

```java
/**
 * @snippet region="example"
 * // @start region="example"
 * int sum(int a, int b) {
 *     return a + b;
 * }
 * // @end region="example"
 */
```

**Назначение регионов:**

- Документация
    
- Встраивание примеров в разные секции
    
- Автоматическая проверка через `javadoc -linksource`
    

### **63.6.5.3 Комментарии к использованию snippet**

|Форма|Назначение|
|---|---|
|`@snippet`|встроить полноценный пример|
|`@highlight`|выделение важной строки|
|`@replace`|показать альтернативу|

✅ Пример:

```java
/**
 * @snippet
 * class Demo {
 *     public static void main(String[] args) {
 *         // @highlight region regex="System.out"
 *         System.out.println("Hello, world!");
 *     }
 * }
 */
```

---

## **63.6.6 Custom Doclets and Tooling**

### **63.6.6.1 Стандартный doclet**

JDK поставляется с _Standard Doclet_, который генерирует HTML-документацию.

### **63.6.6.2 Пользовательские doclet**

Позволяют:

- генерировать PDF, Markdown, JSON-документацию
    
- собирать метаданные API
    
- применять кастомное форматирование
    

```bash
javadoc -doclet com.example.MyDoclet -docletpath out/production/mydoclet ...
```

### **63.6.6.3 Доклеты для подготовки спецификаций**

- **AsciiDoc Doclet** — вывод документации в AsciiDoc для публикаций.
    
- **JSON Doclet** — используется для генерации документации REST API.
    

---

## **63.6.7 Linking and External References**

### **63.6.7.1 `-link` и `-linkoffline`**

Используется для ссылок на внешнюю документацию:

```bash
javadoc -link https://docs.oracle.com/en/java/javase/25/docs/api/
```

### **63.6.7.2 `@see`, `@link`, `@docRoot`**

|Элемент|Пример|Назначение|
|---|---|---|
|`@see`|`@see java.util.List`|добавление ссылки в конец документа|
|`{@link}`|`{@link List#add(Object)}`|встроенная ссылка|
|`{@docRoot}`|`{@docRoot}/overview-summary.html`|относительный путь к корню документации|

✅ Пример:

```java
/**
 * @see java.util.Map
 * @see <a href="https://example.com/protocol">Протокол</a>
 */
```

---

## **63.6.8 Generating Documentation with `javadoc` Tool**

### **63.6.8.1 Базовая команда**

```bash
javadoc -d docs -sourcepath src -subpackages com.example
```

### **63.6.8.2 Полезные опции**

|Опция|Назначение|
|---|---|
|`-d`|директория вывода|
|`-encoding UTF-8`|кодировка исходников|
|`-link`|ссылки на внешнее API|
|`-Xdoclint`|включение строгой проверки|
|`-author` и `-version`|добавление метаданных|
|`--add-modules`|документация модулей|
|`--show-members`|выбор уровня видимости|

### **63.6.8.3 Интеграция с Maven и Gradle**

**Maven:**

```xml
<plugin>
  <groupId>org.apache.maven.plugins</groupId>
  <artifactId>maven-javadoc-plugin</artifactId>
  <version>3.6.0</version>
  <configuration>
    <source>25</source>
    <show>protected</show>
    <detectLinks>true</detectLinks>
  </configuration>
</plugin>
```

**Gradle:**

```groovy
javadoc {
    source = sourceSets.main.allJava
    options.encoding = 'UTF-8'
    options.links("https://docs.oracle.com/en/java/javase/25/docs/api/")
}
```

---

## **63.6.9 Javadoc Quality and Linting**

### **63.6.9.1 Проверка javadoc-lint**

```bash
javadoc -Xdoclint:all -Werror ...
```

**Типы предупреждений:**

- Отсутствие описания параметров
    
- Неупомянутые исключения
    
- Неправильное использование HTML-тегов
    
- Ссылки на несуществующий API
    

### **63.6.9.2 Статические анализаторы**

|Инструмент|Назначение|
|---|---|
|Checkstyle|проверка стиля Javadoc|
|SonarQube|контроль покрытия документацией|
|SpotBugs|обнаружение ошибок API документации|

---

## **63.6.10 Best Practices for Industrial API Documentation**

### ✅ **Делайте:**

- Документируйте _все public и protected методы_
    
- Используйте _императивный стиль_: «Возвращает сумму», а не «Этот метод возвращает сумму»
    
- Описывайте _контракт_: допустимые значения, побочные эффекты
    
- Добавляйте примеры использования (`@snippet`)
    
- Документируйте _исключения и edge cases_
    

### ❌ **Не делайте:**

- Не описывайте реализацию («цикл for»)
    
- Не повторяйте имя метода в описании (`getName()` → «Возвращает имя», а не «Метод getName возвращает имя»)
    
- Не игнорируйте `@throws`
    
- Не используйте Javadoc для TODO
    

---

## **63.6.11 Summary — Резюме ключевых принципов**

|Принцип|Значение|
|---|---|
|Javadoc — часть контракта API|Не документированное поведение ≡ не гарантированное|
|Первый абзац = краткое описание|Отображается в IDE и в оглавлении|
|Используйте стандартные теги|Это делает API самодокументируемым|
|Качество документации = качество API|Javadoc читают чаще, чем исходники|
|Автоматизация|Интегрируйте генерацию в CI/CD|

### **63.6.12 Documenting Classes, Interfaces, Enums, and Annotations**

_(Стиль: промышленное руководство, JDK SE 25, уровень API-спецификации)_

---

# **63.6.12 Documenting Classes, Interfaces, Enums, and Annotations**

## **63.6.12.1 Общие правила документирования типов**

Документация верхнего уровня (`class`, `interface`, `enum`, `@interface`) должна:

- определять **предназначение типа**;
    
- описывать **контракт и область корректного использования**;
    
- фиксировать **ограничения и гарантии**;
    
- указывать **потенциальные побочные эффекты** и **потокобезопасность**.
    

> _Первый абзац Javadoc для типа — это его формальное описание в API-спецификации. Он должен выражать сущность типа, а не детали его реализации._

✅ Формат:

```java
/**
 * <краткое определение назначения типа (1 предложение)>.
 *
 * <расширенное описание, включающее контракт, ограничения, гарантии выполнения,
 * особенности потокобезопасности, версионные условия, взаимодействие с другими типами>.
 *
 * @since 25
 * @apiNote <дополнительные рекомендации по использованию>
 * @implSpec <если интерфейс или абстрактный класс>
 * @implNote <если нужно отметить детали реализации>
 */
```

---

## **63.6.12.2 Документирование классов**

### ✔ Обязательные компоненты:

|Элемент|Назначение|
|---|---|
|Краткое описание|Что представляет класс (не «класс X является…», а «Представляет…»).|
|Контракт|Гарантии, ответственность вызывающей стороны.|
|Thread-safety|Указать: immutable, thread-safe, not thread-safe, conditionally thread-safe.|
|Version Info|`@since`, `@deprecated`|
|Комментарии реализации|Через `@implNote`, если необходимо.|

### ✅ Пример (корректно):

```java
/**
 * Представляет неизменяемый денежный тип с фиксированной валютой.
 *
 * <p>Экземпляры этого класса являются thread-safe и могут быть безопасно
 * использованы в многопоточной среде без дополнительной синхронизации.
 *
 * @since 25
 * @apiNote Значения следует создавать с использованием фабричного метода {@link #of}.
 */
public final class Money { ... }
```

---

## **63.6.12.3 Документирование интерфейсов**

Особенность интерфейсов: они определяют **API-контракт**, а не реализацию.

- Используйте `@implSpec` для описания требований к реализациям.
    
- Используйте `@apiNote` для рекомендаций клиентам API.
    

```java
/**
 * Определяет контракт для объектов, поддерживающих конвертацию в байтовый массив.
 *
 * @implSpec Реализации обязаны гарантировать, что повторные вызовы {@code toBytes()}
 * возвращают одинаковый результат.
 */
public interface SerializableEntity { ... }
```

---

## **63.6.12.4 Документирование `enum` типов**

Enum — часть API, **обязателен Javadoc для каждого элемента**.

```java
/**
 * Представляет уровень логирования в системе.
 *
 * @since 25
 */
public enum LogLevel {
    /** Ошибки, приводящие к отказу системы. */
    ERROR,

    /** Предупреждения, указывающие на потенциальные проблемы. */
    WARN,

    /** Информационные сообщения. */
    INFO;

    // Дополнительные методы с документацией
}
```

---

## **63.6.12.5 Документирование аннотаций (`@interface`)**

Аннотации описывают **метаданные**, поэтому документирование обязательно.

```java
/**
 * Обозначает, что элемент является экспериментальным и может быть изменён или
 * удален в будущих версиях.
 *
 * @apiNote Не рекомендуется для использования в production-коде.
 * @since 24
 */
@Retention(RetentionPolicy.RUNTIME)
@Target({ElementType.TYPE, ElementType.METHOD})
public @interface Experimental { }
```

Особенно важно документировать:

|Элемент аннотации|Документация|
|---|---|
|Семантика применения|Кому следует применять и зачем|
|Retention policy|Влияет на runtime поведение|
|Target|Ограничения по использованию|
|Default значения элементов|Объяснить их смысл|

---

## **63.6.12.6 Потокобезопасность и жизненный цикл**

Каждый тип должен содержать сведения о:

- **Thread-safety**
    
- **Mutability**
    
- **Lifecycle contract** (например, “экземпляры должны быть закрыты после использования”)
    

Шаблон:

```java
/**
 * ...
 * <h2>Thread Safety</h2>
 * Этот класс является потокобезопасным: все операции синхронизированы.
 *
 * <h2>Lifecycle</h2>
 * После вызова {@link #close()} экземпляр переходит в состояние "закрыт"
 * и дальнейшее использование приводит к {@link IllegalStateException}.
 */
```

---

## **63.6.12.7 Итоговые правила раздела 63.6.12**

|Правило|Статус|
|---|---|
|Каждый тип верхнего уровня должен быть документирован|✅ Обязательно|
|Первый абзац = контракт типа|✅|
|Для интерфейсов и абстрактных классов использовать `@implSpec`|✅|
|Для enum — документировать каждый элемент|✅|
|Для аннотаций — документировать семантику, retention и target|✅|
|Thread-safety и lifecycle должны быть явно указаны|✅|

### **63.6.13 Documenting Constructors, Fields, and Constants**

Документация этих элементов часто недооценивается, но именно они определяют, как тип можно инициировать, конфигурировать и использовать. В промышленной практике корректная документация конструкторов и полей — обязательное требование для API уровня Java SE.

---

# **63.6.13.1 Documenting Constructors — Документирование конструкторов**

## **Назначение:**

Конструктор устанавливает **контракт создания объекта**. Он обязан описывать:

- что требуется от вызывающего кода;
    
- какие ограничения накладываются на входные параметры;
    
- какие побочные эффекты возникают;
    
- какие исключения могут быть выброшены;
    
- гарантии валидного состояния объекта после создания.
    

✅ **Шаблон:**

```java
/**
 * Создает новый экземпляр буфера фиксированного размера.
 *
 * @param capacity максимальный размер буфера; должен быть > 0
 * @throws IllegalArgumentException если capacity <= 0
 * @apiNote Данный конструктор выделяет память, которая не может быть освобождена автоматически до вызова {@link #close()}.
 */
public Buffer(int capacity) {
    ...
}
```

### ✔ Best Practices:

|Требование|Обоснование|
|---|---|
|Документируй каждый публичный конструктор|Конструктор — часть API|
|Используй @throws для валидации параметров|Контракт API|
|Указывай побочные эффекты|Например: создание файлов, регистрация потоков|
|Указывай @apiNote для особых случаев|Например: «используйте фабрику вместо конструктора»|

---

# **63.6.13.2 Documenting Fields — Документирование полей**

## **Классификация полей:**

|Тип поля|Документация требуется|Комментарий|
|---|---|---|
|`public`|✅ Обязательно|Часть API|
|`protected`|✅ Желательно|Для наследников|
|`private`|❌ Не документируется Javadoc'ом|Используются inline-комментарии|

## **Документирование public/protected полей:**

```java
/**
 * Максимально допустимое количество подключений.
 * Значение по умолчанию: 100.
 * <p>Это поле является конфигурационным и может быть изменено перед вызовом {@link #start()}.</p>
 */
protected int maxConnections = 100;
```

### ❗ Примечания:

- Поля должны использоваться **только для состояния**, не для поведения.
    
- Документируйте **жизненный цикл**: когда можно изменять и на что он влияет.
    
- Указывайте **thread-safety**: поле может быть `volatile` или требовать синхронизации.
    

---

# **63.6.13.3 Documenting Constants (static final)**

Константы являются частью _официального API_ и часто используются сторонними разработчиками.

✅ **Шаблон:**

```java
/**
 * Значение по умолчанию для таймаута соединения (в миллисекундах).
 * <p>Используется, если таймаут не был указан явно.</p>
 */
public static final int DEFAULT_TIMEOUT = 5000;
```

### ✔ Рекомендации:

|Правило|Причина|
|---|---|
|Документируй смысл, не имя|Имя класса может измениться, документация останется|
|Используй {@value}|Чтобы отобразить значение прямо в документе|
|Не включай реализацию|`DEFAULT_TIMEOUT = 5000 // устанавливается из конфигурации` ❌|

✅ **Пример с `@value`:**

```java
/**
 * Минимально допустимый размер буфера (в байтах).
 * @value
 */
public static final int MIN_BUFFER_SIZE = 256;
```

---

# **63.6.13.4 Документирование Enum Constants**

Каждый элемент enum описывает **константу поведения**:

```java
/**
 * Уровень важности журнала событий.
 */
public enum LogLevel {
    /** Критические ошибки, требующие немедленного вмешательства. */
    FATAL,
    /** Ошибки, влияющие на выполнение, но допускающие продолжение работы. */
    ERROR,
    /** Информационные события. */
    INFO
}
```

---

# **63.6.13.5 Common Mistakes to Avoid — Ошибки, которых следует избегать**

|Ошибка|Почему опасно|
|---|---|
|Документация повторяет имя конструктора/поля|Утрачивается ценность Javadoc|
|Отсутствие указания условий и ограничений|Пользователь API не знает, как правильно использовать|
|Игнорирование `@throws`|Нарушение контракта API|
|Отсутствие жизненного цикла|Неочевидно, когда и что можно менять|

---

# **63.6.13.6 Резюме ключевых принципов**

|Элемент|Что обязательно документировать|
|---|---|
|Конструктор|Цель, параметры, исключения, побочные эффекты|
|Поля|Семантика, влияние на объект, жизненный цикл|
|Константы|Назначение, значение, область применения|


### **63.6.14 Documenting Methods — Contracts, Exceptions, Thread Safety**

Методы являются основной точкой взаимодействия API с пользователем. Их документация формирует **контракт использования**, определяет гарантии и ограничения. Ошибки или пробелы в описании метода приводят к некорректному использованию API, неочевидным багам и нарушению обратной совместимости.

---

# **63.6.14.1 Structural Requirements — Структура описания метода**

Каждый публичный или защищённый метод должен включать:

1. **Краткое описание поведения**
    
2. **Подробное описание (если требуется):**
    
    - условия, побочные эффекты
        
    - порядок вызова
        
    - пред- и постусловия (preconditions/postconditions)
        
3. **Описание каждого параметра**
    
4. **Описание возвращаемого значения**
    
5. **Список исключений (`@throws`)**
    
6. **Информация о потокобезопасности**
    
7. **Ссылки и советы по использованию** (`@apiNote`, `@implSpec`)
    

✅ **Базовый шаблон:**

```java
/**
 * Возвращает элемент списка по индексу.
 *
 * @param index индекс элемента, начиная с 0
 * @return элемент списка
 * @throws IndexOutOfBoundsException если index < 0 или index >= size()
 * @implSpec Метод должен завершаться за постоянное время.
 * @apiNote Этот метод является частью контракта интерфейса List.
 */
E get(int index);
```

---

# **63.6.14.2 Documenting Method Contracts — Контракты и гарантии**

Контракт описывает, **какие условия должны быть выполнены** и **что гарантируется пользователю API**.

### **Preconditions — Предусловия**

- Условия, которые **обязан выполнить вызывающий код**
    
- Нарушение приводит к ошибке или `IllegalArgumentException`
    

✅ Пример:

```java
 * @throws NullPointerException если element == null
```

### **Postconditions — Постусловия**

- Гарантии, которые предоставляет метод после завершения
    

✅ Пример:

```java
 * @return новая неизменяемая коллекция, содержащая элементы в порядке сортировки
```

### **Side Effects — Побочные эффекты**

- Изменение состояния объекта
    
- Взаимодействие с внешними системами (файлы, сеть, БД)
    

✅ Пример:

```java
 * @implNote Метод записывает данные на диск и может вызвать задержку IO.
```

---

# **63.6.14.3 Documenting Exceptions — Исключения как часть API**

Каждое исключение должно быть **задокументировано** как **контракт**:

|Тип исключения|Роль|Документировать?|
|---|---|---|
|Checked (`IOException`)|Обязателен к обработке|✅ Да|
|Unchecked (`NullPointerException`, `IllegalArgumentException`)|Сигнал о неправильном использовании API|✅ Да|
|Errors (`OutOfMemoryError`)|Нежелательные, не документируются|🚫 Нет|

✅ Пример:

```java
/**
 * Загружает файл конфигурации.
 *
 * @param path путь к файлу
 * @throws IOException если файл не найден или недоступен
 * @throws SecurityException если доступ запрещён политикой безопасности
 */
void load(Path path) throws IOException;
```

---

# **63.6.14.4 Thread Safety and Concurrency**

В многопоточных (concurrent) API **обязательно** указывать уровень потокобезопасности метода:

|Аннотация|Объяснение|
|---|---|
|**Thread-safe**|можно вызывать из разных потоков одновременно|
|**Not thread-safe**|вызывающий код обязан синхронизировать|
|**Conditionally thread-safe**|безопасность зависит от конфигурации|
|**Locking behavior described**|например: «метод синхронизирован на this»|

✅ Пример:

```java
/**
 * Добавляет элемент в коллекцию.
 * <p><strong>Потокобезопасность:</strong> метод не является потокобезопасным
 * и должен вызываться только при внешней синхронизации.</p>
 */
void add(E element);
```

---

# **63.6.14.5 Method Overloads and Default Methods**

### ✅ Документируйте **каждый перегруженный метод отдельно**, даже если отличается только типом параметра.

### ✅ Для интерфейсов с default-методами **используйте @implSpec**:

```java
/**
 * {@inheritDoc}
 *
 * @implSpec Реализация по умолчанию вызывает {@link #remove(Object)}.
 */
default boolean removeIf(Predicate<? super E> filter) {
    ...
}
```

---

# **63.6.14.6 Examples and Usage Notes**

Добавление примеров повышает понятность API:

```java
/**
 * @apiNote
 * Пример:
 * <pre>{@code
 * Cache cache = new Cache(100);
 * cache.put("k", "v");
 * }</pre>
 */
```

---

# **63.6.14.7 Summary — Метод является контрактом**

|Что документировать|Почему важно|
|---|---|
|Параметры и возвращаемое значение|Определяет API|
|Пред- и постусловия|Обеспечивают корректное использование|
|Исключения|Гарантируют предсказуемость поведения|
|Thread safety|Определяет допустимые сценарии многопоточности|
|Поведение по умолчанию|Обязательства реализации|

---

### **63.6.15 Documenting Annotations and Functional Interfaces**

Аннотации и функциональные интерфейсы представляют **особые формы API**, которые требуют специфической документации. Недостаточная документация этих типов приводит к неверному использованию, что особенно критично при проектировании фреймворков, библиотек и DSL на базе Java.

---

# **63.6.15.1 Documenting Annotations — Документация аннотаций**

Аннотации сами являются типами (`@interface`), и их Javadoc определяет **контракт применения**, **область действия**, **влияние на код**, **поведение при обработке**.

### ✅ Структура документации аннотации

```java
/**
 * Указывает, что метод является потокобезопасным.
 *
 * <p>Эта аннотация используется фреймворками для генерации мониторинга и
 * валидации потокобезопасности на этапе выполнения.</p>
 *
 * @apiNote Применяется только к методам экземпляра.
 */
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface ThreadSafe {
}
```

### **Обязательные элементы для описания:**

|Что описать|Зачем|
|---|---|
|Назначение|Задает смысл применения|
|@Target|Какие элементы кода можно аннотировать|
|@Retention|Где аннотация доступна (source, class, runtime)|
|@Repeatable|Можно ли использовать несколько раз|
|@Documented|Отображается ли в Javadoc по умолчанию|

---

### **63.6.15.1.1 Документирование элементов аннотации**

```java
/**
 * Определяет роль компонента в контейнере внедрения зависимостей.
 *
 * @author ...
 * @since 1.0
 */
@Target(ElementType.TYPE)
@Retention(RetentionPolicy.RUNTIME)
public @interface Component {

    /**
     * Имя компонента.
     *
     * @return уникальный идентификатор компонента в контейнере
     */
    String value() default "";
}
```

> Каждый элемент аннотации (`value()`) **должен быть задокументирован как метод**, включая описание, контракт и значение по умолчанию.

---

# **63.6.15.2 Best Practices for Annotation Documentation**

✅ Укажите, кто и что будет использовать аннотацию (runtime, компилятор, фреймворк).  
✅ Опишите влияние на поведение.  
✅ Укажите возможные ошибки при неправильном применении.  
✅ Добавляйте `@implSpec`, если аннотация влияет на байткод.

---

# **63.6.15.3 Documenting Functional Interfaces**

Функциональные интерфейсы (с одним абстрактным методом) являются основой лямбда-выражений и stream API.

### ✅ Структура документации функционального интерфейса

```java
/**
 * Представляет функцию, принимающую аргумент и возвращающую результат.
 *
 * <p>Функциональный интерфейс, использующийся в Stream API и
 * методах высшего порядка.</p>
 *
 * @param <T> тип входного аргумента
 * @param <R> тип результата
 * @functionalInterface
 * @see java.util.function.Function
 */
@FunctionalInterface
public interface Transformer<T, R> {

    /**
     * Преобразует входной аргумент.
     *
     * @param input входное значение
     * @return результат преобразования (не может быть null)
     * @throws IllegalArgumentException если input некорректен
     */
    R apply(T input);
}
```

---

# **63.6.15.3.1 Обязательные элементы для функциональных интерфейсов**

|Что документировать|Описание|
|---|---|
|Цель интерфейса|Что именно абстрагирует интерфейс|
|Единственный абстрактный метод|Описание входных и выходных значений|
|Контракт nullability|Можно ли передавать или возвращать `null`|
|Thread-safety|Является ли реализация потокобезопасной|
|Side effects|Допускается ли изменение состояния при реализации|

---

# **63.6.15.3.2 Документирование лямбда-использований**

```java
/**
 * @apiNote Типичный сценарий:
 * <pre>{@code
 * Transformer<String, Integer> length = s -> s.length();
 * }</pre>
 */
```

---

# **63.6.15.4 Anti-patterns**

|Антипаттерн|Почему плохо|Правильный подход|
|---|---|---|
|Отсутствие описания назначения аннотации|Пользователь не понимает, когда её применять|Добавить краткое и точное описание|
|Нет документации для элементов аннотации|Затрудняет понимание параметров|Документировать каждый элемент как метод|
|Не указаны ограничения применения|Пользователь применяет неправильно|Документировать @Target и контракт|
|Функциональный интерфейс без @FunctionalInterface|Нет гарантии от ошибок|Добавить аннотацию и описать контракт|

---

# **63.6.15.5 Summary**

|Объект|Документировать|Обязательно|
|---|---|---|
|Аннотация|Назначение, область применения, жизненный цикл|Да|
|Элемент аннотации|Тип, поведение, значение по умолчанию|Да|
|Функциональный интерфейс|Его роль в функциональном программировании|Да|
|Абстрактный метод|Контракт API для лямбда-реализаций|Да|

### **63.6.16 Documenting Classes, Records, and Enums**

Этот раздел предоставляет стандартизированные правила документирования _структур данных и типов верхнего уровня_, включая **обычные классы, enum, record**, а также **вложенные классы**. Для всех этих структур Javadoc должен описывать **назначение, инварианты, жизненный цикл, правила использования и ограничения**.

---

# **63.6.16.1 Documenting Classes — Документирование классов**

Документация класса описывает **его роль в системе**, **контракт**, **инварианты**, **типичные сценарии использования**, и **ограничения**.

### ✅ Обязательные элементы Javadoc для классов:

|Что документировать|Описание|
|---|---|
|Назначение класса|Что представляет/реализует класс? Какую задачу решает?|
|Инварианты|То, что всегда истинно для объектов данного класса|
|Потокобезопасность|Является ли класс immutable, thread-safe, или требует внешней синхронизации|
|Ограничения|Что не допускается? Какие есть побочные эффекты?|
|Примеры использования|Чёткий, валидный фрагмент кода (`@snippet` или `{@code}`)|

### ✅ Пример качественной документации:

```java
/**
 * Immutable класс, представляющий денежную сумму в фиксированной валюте.
 *
 * <p>Экземпляры этого класса являются неизменяемыми и потокобезопасными.
 * Операции сложения и вычитания создают новые объекты.</p>
 *
 * <h2>Инварианты:</h2>
 * <ul>
 *   <li>amount >= 0</li>
 *   <li>currency != null</li>
 * </ul>
 *
 * @apiNote Этот класс используется в платежных системах и поддерживает точность до цента.
 *
 * @implSpec Все операции должны выполнять арифметику с использованием {@code BigDecimal}.
 *
 * @since 1.0
 */
public final class Money {
    private final BigDecimal amount;
    private final Currency currency;
}
```

---

# **63.6.16.2 Documenting Records — Документирование record-типов**

`record` автоматически генерирует `equals`, `hashCode`, `toString`, но документация **является обязательной**, так как `record` используется как _семантический тип данных_.

### ✅ Принципы документирования record:

|Правило|Описание|
|---|---|
|Документировать сам record|Назначение, инварианты|
|Документировать каждый компонент конструктора|Как Javadoc для поля или параметра|
|Указать неизменяемость и потокобезопасность|Record по умолчанию immutable|

### ✅ Пример:

```java
/**
 * Представляет результат расчета налога.
 *
 * <p>Объекты этого record неизменяемы и потокобезопасны.</p>
 *
 * @param baseAmount базовая сумма, к которой применяется налог (должна быть >= 0)
 * @param taxAmount значение налога
 * @param totalAmount итоговая сумма с учетом налога
 */
public record TaxResult(BigDecimal baseAmount, BigDecimal taxAmount, BigDecimal totalAmount) {
    public TaxResult {
        if (baseAmount.signum() < 0)
            throw new IllegalArgumentException("baseAmount must be >= 0");
    }
}
```

> **Если record содержит валидацию или бизнес-логику, это обязательно должно быть упомянуто в Javadoc.**

---

# **63.6.16.3 Documenting Enums — Документирование enum-типов**

Enum — это **тип с фиксированным набором допустимых значений** и **потенциальным поведением**. Документация должна описывать:

|Что документировать|Описание|
|---|---|
|Назначение enum|Что моделирует?|
|Каждый элемент enum|Семантика этого значения|
|Методы enum|Какое поведение добавляет enum?|

✅ Пример:

```java
/**
 * Представляет статус платежа в системе.
 *
 * <p>Каждое значение этого перечисления соответствует конечному состоянию бизнес-процесса.</p>
 *
 * @apiNote Значение {@link #FAILED} не может быть изменено на {@link #COMPLETED}.
 */
public enum PaymentStatus {
    /**
     * Платеж создан, но еще не подтвержден.
     */
    CREATED,

    /**
     * Платеж успешно завершен.
     */
    COMPLETED,

    /**
     * Платеж не прошел проверку или отклонен.
     */
    FAILED
}
```

---

# **63.6.16.4 Documenting Nested and Static Classes**

Вложенные классы могут быть:

- private helper
    
- public part of API
    
- static holder classes
    

📌 **Правило**: если вложенный класс является частью публичного API — он документируется **наравне с обычным классом**.

```java
/**
 * Содержит вспомогательные методы для работы с массивами.
 */
public class ArrayUtils {

    /**
     * Набор предопределённых стратегий сортировки.
     */
    public static class SortingStrategy {
        // ...
    }
}
```

---

# **63.6.16.5 Best Practices**

✅ Описание должно объяснять **«что делает тип»**, а не «как он реализован»  
✅ Использовать термины **«immutable», «thread-safe», «not thread-safe», «IO-bound», «functional»**  
✅ Указывать ограничения:

> _"Этот класс не предназначен для использования в распределённых системах"_

✅ Для классов с побочными эффектами обязательно использовать `@implNote`

---

# **63.6.16.6 Типичные ошибки**

|Ошибка|Последствие|
|---|---|
|Документирование реализационных деталей|Пользователь API начинает зависеть от внутренней реализации|
|Отсутствие описания инвариантов|Нарушение контракта при неправильном использовании|
|Недокументированные enum значения|Бизнес-логика становится неочевидной|
|Игнорирование thread-safety|Гонка данных и ошибки в многопоточных приложениях|

---

# **63.6.16.7 Заключение**

- Классы, records и enums должны документироваться как _контракты_, а не как _код_.
    
- Документация должна объяснять **что гарантируется и что запрещено**.
    
- Для неизменяемых структур важно явно указывать их **immutability contract**.
    

---
### **63.6.17 Advanced Documentation Techniques — Расширенные техники документирования**

Этот раздел предназначен для описания **сложных случаев документирования API**, которые встречаются в промышленной разработке: перегрузки, переопределения, дженерики, контракт потокобезопасности, документирование сложностей производительности и сторонних эффектов. Правильное описание этих аспектов напрямую влияет на корректное использование классов и методов в реальных приложениях.

---

# **63.6.17.1 Documenting Method Overloads — Документирование перегрузок методов**

Перегруженные методы должны **иметь уникальную документацию**, если:

- поведение перегрузок отличается,
    
- разные параметры приводят к различным эффектам,
    
- есть ограничения по типу или диапазону значений.
    

💡 **Правило**: _никогда не копируйте Javadoc дословно между перегрузками, если есть различия._

✅ Пример:

```java
/**
 * Читает весь файл в массив байтов.
 *
 * @param path путь к файлу
 * @return содержимое файла
 */
public byte[] read(Path path);

/**
 * Читает часть файла.
 *
 * @param path путь к файлу
 * @param bufferSize максимальный размер буфера, в байтах
 * @return массив байтов размером не больше bufferSize
 * @throws IllegalArgumentException если bufferSize <= 0
 */
public byte[] read(Path path, int bufferSize);
```

---

# **63.6.17.2 Documenting Overrides — Переопределения и `@inheritDoc`**

Если метод **полностью наследует поведение**, используйте `{@inheritDoc}`:

```java
/**
 * {@inheritDoc}
 *
 * @implNote Этот метод оптимизирован и не создает временных объектов.
 */
@Override
public int size() {
    return this.count;
}
```

Если поведение **меняется** — его нужно **описать заново**:

```java
/**
 * Возвращает неизменяемый список.
 *
 * <p>В отличие от {@link MutableList#subList(int, int)}, этот метод
 * возвращает новый объект, не зависящий от исходного списка.</p>
 */
@Override
public List<E> subList(int fromIndex, int toIndex) { ... }
```

---

# **63.6.17.3 Documenting Generics — Документация обобщённых типов**

### ✅ Обязательные элементы:

|Элемент|Тег|Описание|
|---|---|---|
|Параметры типа|`@param <T>`|Что представляет тип|
|Ограничения|`extends`, `super`|Какой тип допустим|
|Контракт nullability|`nullable`, `non-null` контракты||

```java
/**
 * Кэш значений с автоматической генерацией.
 *
 * @param <K> тип ключа (должен быть immutable)
 * @param <V> тип значения
 */
public interface Cache<K, V> {
    V get(K key);
}
```

---

# **63.6.17.4 Documenting Nullability and Optional Usage**

Java не проверяет null на уровне компилятора. Поэтому **Javadoc должен явно указывать**, допускаются ли `null`-значения:

|Фраза|Смысл|
|---|---|
|`@param x может быть null`|null допустим|
|`@throws NullPointerException если x == null`|null запрещён|
|`@return никогда не возвращает null`|гарантия|

✅ Пример:

```java
/**
 * @param userName не может быть null
 * @return объект пользователя, никогда не null
 * @throws NullPointerException если userName == null
 */
public User findUser(String userName);
```

---

# **63.6.17.5 Thread-safety and Concurrency Contracts**

Документация должна указывать:

|Уровень безопасности|Описание|
|---|---|
|Immutable|Объект неизменяем, безопасен без синхронизации|
|Thread-safe|Может безопасно использоваться из нескольких потоков|
|Not thread-safe|Требуется внешняя синхронизация|
|Conditionally thread-safe|Некоторые методы потокобезопасны|

✅ Пример:

```java
/**
 * Thread-safe реализация счетчика.
 *
 * <p>Все операции инкремента являются атомарными.</p>
 *
 * @implSpec Использует AtomicInteger.
 */
public class AtomicCounter { ... }
```

---

# **63.6.17.6 Documenting Performance Guarantees (Big O)**

Промышленные API **обязаны указывать сложность операций**, если от этого зависит поведение системы.

```java
/**
 * Добавляет элемент в конец списка.
 *
 * <p>Операция выполняется за амортизированное O(1).</p>
 */
boolean add(E e);
```

---

# **63.6.17.7 Documenting Side Effects and Mutability**

```java
/**
 * Изменяет внутреннее состояние объекта.
 *
 * @apiNote Этот метод имеет побочные эффекты и не является idempotent.
 */
void update();
```

---

# **63.6.17.8 Summary**

|Аспект|Обязателен к документированию в API|
|---|---|
|Перегрузки|Да, если поведение различается|
|Nullability|Да, всегда|
|Thread-safety|Да|
|Performance|Да, если влияет на решения пользователя|
|Generics|Документировать `<T>`|
|Side effects|Указывать|

---
### **63.6.18 Full Example — Полностью документированный класс (Эталон промышленного уровня)**

Этот раздел демонстрирует **идеальный образец документированного API-класса**, соответствующий стандартам Java SE, Oracle API Guidelines, Google Java Style и корпоративным требованиям. В примере показано:

- класс;
    
- generics;
    
- thread-safety;
    
- immutability;
    
- перегрузки;
    
- исключения;
    
- использование `@apiNote`, `@implNote`, `@implSpec`, `@since`;
    
- `@snippet`, `@link`, `@see`;
    
- полная Javadoc-структура.
    

---

# **✅ Пример: `Cache` — потокобезопасный кэш с автоматическим вычислением значений**

```java
package com.example.cache;

import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;
import java.util.function.Function;

/**
 * Потокобезопасный кэш с ленивым вычислением значений.
 *
 * <p>Класс {@code Cache} предоставляет механизм хранения пар "ключ-значение" с возможностью
 * автоматической генерации значения при его отсутствии. Все операции потокобезопасны
 * и выполняются с использованием {@link ConcurrentHashMap}.</p>
 *
 * <h2>Пример использования</h2>
 * {@snippet :
 * Cache<String, Integer> cache = Cache.create(key -> key.length());
 * int result = cache.get("Java"); // 4
 * }
 *
 * <p><strong>Инварианты:</strong></p>
 * <ul>
 *   <li>Ключ не может быть {@code null}</li>
 *   <li>Функция генерации значения всегда должна возвращать не-null результат</li>
 * </ul>
 *
 * @param <K> тип ключа (должен быть immutable или корректно переопределять equals/hashCode)
 * @param <V> тип значения (должен быть thread-safe или immutable)
 *
 * @apiNote Кэш является <em>потокобезопасным</em> и поддерживает многопоточную запись.
 * @implSpec Реализация использует {@link ConcurrentHashMap#computeIfAbsent(Object, Function)}.
 * @since 1.0
 */
public final class Cache<K, V> {

    private final Map<K, V> storage;
    private final Function<K, V> valueProvider;

    /**
     * Приватный конструктор. Используйте {@link #create(Function)}.
     *
     * @param valueProvider функция, вычисляющая значения по ключу
     * @throws NullPointerException если valueProvider равен null
     */
    private Cache(Function<K, V> valueProvider) {
        if (valueProvider == null) {
            throw new NullPointerException("valueProvider cannot be null");
        }
        this.storage = new ConcurrentHashMap<>();
        this.valueProvider = valueProvider;
    }

    /**
     * Создаёт новый экземпляр кэша с заданной функцией-вычислителем значений.
     *
     * @param <K> тип ключа
     * @param <V> тип значения
     * @param valueProvider функция, вычисляющая значение по ключу (не может быть null)
     * @return новый экземпляр кэша
     * @throws NullPointerException если valueProvider равен null
     *
     * @apiNote Этот метод является <em>фабричным</em> и возвращает потокобезопасный кэш.
     * @see #get(Object)
     */
    public static <K, V> Cache<K, V> create(Function<K, V> valueProvider) {
        return new Cache<>(valueProvider);
    }

    /**
     * Возвращает значение, связанное с указанным ключом. Если значение отсутствует,
     * оно будет вычислено с использованием {@code valueProvider}.
     *
     * <p>Операция атомарна и потокобезопасна. Если несколько потоков одновременно
     * запрашивают значение по одному ключу, оно будет вычислено только один раз.</p>
     *
     * @param key ключ (не может быть null)
     * @return значение, никогда не null
     * @throws NullPointerException если key равен null
     * @throws IllegalStateException если valueProvider вернул null
     *
     * @implSpec Реализация использует {@link ConcurrentHashMap#computeIfAbsent(Object, Function)}.
     */
    public V get(K key) {
        if (key == null) {
            throw new NullPointerException("key cannot be null");
        }
        V value = storage.computeIfAbsent(key, valueProvider);
        if (value == null) {
            throw new IllegalStateException("valueProvider returned null");
        }
        return value;
    }

    /**
     * Проверяет, содержит ли кэш значение по указанному ключу.
     *
     * @param key ключ, который нужно проверить (может быть null)
     * @return true, если значение содержится в кэше; false — в противном случае
     */
    public boolean contains(K key) {
        return storage.containsKey(key);
    }

    /**
     * Удаляет значение из кэша.
     *
     * @param key ключ (не может быть null)
     * @return удалённое значение или null, если ключ не был найден
     * @throws NullPointerException если key равен null
     */
    public V remove(K key) {
        if (key == null) {
            throw new NullPointerException("key cannot be null");
        }
        return storage.remove(key);
    }

    /**
     * Возвращает текущее количество элементов в кэше.
     *
     * @return количество элементов
     *
     * @implNote Размер может меняться при многопоточном доступе.
     */
    public int size() {
        return storage.size();
    }

    /**
     * Очищает кэш.
     *
     * @implNote Метод имеет побочный эффект и не является потокобезопасным
     * относительно итераторов, полученных ранее.
     */
    public void clear() {
        storage.clear();
    }

    /**
     * Возвращает строковое представление кэша.
     *
     * @return строка вида {key1=value1, key2=value2, ...}
     */
    @Override
    public String toString() {
        return storage.toString();
    }
}
```

# **63.6.18.1 Ключевые моменты из примера**

|Фича|Присутствует|Где используется|
|---|---|---|
|Императивное описание методов|✔|Первый абзац каждого метода|
|Теги `@param`, `@return`, `@throws`|✔|Каждый метод|
|Generics и Javadoc для `<T>`|✔|Описание параметров типа|
|Thread-safety|✔|Указано в описании класса|
|Иммутабельность|✔|Класс `final`, поля `final`|
|Использование HTML-разметки|✔|Для блоков `<p>`, `<ul>`|
|Snippet API|✔|Пример использования класса|
|API контракт (`@apiNote`, `@implSpec`, `@implNote`)|✔|Во многих элементах|
|Валидация входных данных|✔|С описанием исключений|

