2025-10-29 19:32
Tags: #toFormat 


Jackson поддерживает _полиморфизм_ — возможность сериализовать и десериализовать объекты по их фактическому типу, даже если переменная имеет ссылку на базовый класс или интерфейс.

Однако **полиморфная десериализация — один из самых опасных механизмов**, поскольку при неправильном использовании может привести к выполнению произвольного кода (Remote Code Execution, RCE). Поэтому этот раздел сочетает **функциональность и безопасность**, что критично для реального продакшн-кода.

---

# 📌 **58.3.5.1. Проблема полиморфизма по умолчанию**

JSON не содержит информации о типе:

```json
{ "name": "Rex" }
```

Если JSON десериализуется в интерфейс:

```java
Animal animal = mapper.readValue(json, Animal.class);
```

➡ Jackson **не знает, какой класс создать** → будет выброшено исключение `InvalidDefinitionException`.

---

# 🎯 **58.3.5.2. Включение информации о типе через @JsonTypeInfo**

```java
@JsonTypeInfo(
    use = JsonTypeInfo.Id.CLASS, // или NAME
    include = JsonTypeInfo.As.PROPERTY,
    property = "@class" // имя метаданных
)
abstract class Animal { }

class Dog extends Animal {
    public String name;
}
```

JSON будет включать метаданные:

```json
{
  "@class": "com.example.Dog",
  "name": "Rex"
}
```

Jackson создаст `Dog`, а не `Animal`.

---

# **58.3.5.3. Перечисление допустимых типов (безопасный способ)**

⚠ Никогда не допускайте `@class` с произвольным путём — это потенциальная RCE уязвимость.

✅ Используйте `JsonTypeInfo.Id.NAME` + `@JsonSubTypes`:

```java
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, include = JsonTypeInfo.As.PROPERTY, property = "type")
@JsonSubTypes({
    @JsonSubTypes.Type(value = Dog.class, name = "dog"),
    @JsonSubTypes.Type(value = Cat.class, name = "cat")
})
abstract class Animal {}
```

JSON:

```json
{ "type": "dog", "name": "Rex" }
```

➡ Поддерживается только заранее заданный набор типов.

---

# **58.3.5.4. Пользовательские типовые имена @JsonTypeName**

```java
@JsonTypeName("dog")
class Dog extends Animal {
    public String name;
}
```

➡ JSON:

```json
{ "type": "dog", "name": "Rex" }
```

---

# **58.3.5.5. Полиморфизм и коллекции**

```java
List<Animal> animals = mapper.readValue(json,
    new TypeReference<List<Animal>>() {});
```

Jackson автоматически применяет правила @JsonTypeInfo внутри коллекций.

---

# **58.3.5.6. Безопасность: основные угрозы**

### ❌ Угроза 1: RCE через Gadget Chains

```json
{ "@class": "org.apache.commons.collections.functors.InvokerTransformer", "iMethodName": "exec", "iParamTypes": "...", ... }
```

ℹ Такие цепочки позволяют злоумышленнику выполнять код на сервере при десериализации.

✅ Решение:

- **Никогда не использовать `JsonTypeInfo.Id.CLASS` с входящими JSON от пользователей**
    
- Ограничивать типы с помощью `activateDefaultTyping` + `BasicPolymorphicTypeValidator`
    

---

# **58.3.5.7. Безопасная настройка ObjectMapper**

```java
PolymorphicTypeValidator ptv = BasicPolymorphicTypeValidator.builder()
    .allowIfSubType("com.myapp.model.") // разрешаем только пакет приложения
    .build();

mapper.activateDefaultTyping(ptv, ObjectMapper.DefaultTyping.NON_FINAL, JsonTypeInfo.As.PROPERTY);
```

✅ ObjectMapper теперь не позволит десериализовать произвольный класс извне.

---

# **58.3.5.8. Edge Cases**

### ⚠ Конфликт с @JsonIgnore

Если поле с типовой информацией игнорируется – полиморфизм ломается.

### ⚠ При сериализации без десериализации

Можно сериализовать с типом, но не задать правила десериализации → ошибка.

### ⚠ Наследование через абстрактные классы

Jackson может создать ошибку, если подкласс не имеет конструктора по умолчанию.

---

# ✅ **58.3.5.9. Полезный паттерн: типовое поле в JSON API**

```java
interface Animal { }

class Dog implements Animal {
    public String type = "dog";
    public String name;
}
```

➡ Полиморфизм контролируется приложением, а не JSON. Это **рекомендуется для API**.


#### **58.3.5.10. Безопасные дефолты и «запреты»**

- Используйте `Id.NAME` + **жёсткий белый список** (`@JsonSubTypes` или ручной роутинг по `type`).
    
- Если требуется `activateDefaultTyping`, только с `BasicPolymorphicTypeValidator` и ограничением пакетов.
    
- Обрезайте вход по размеру на уровне HTTP/IO.
    
- Можно запретить опасные типы MixIn’ом:

```java
@JsonIgnoreType
abstract class DangerousTypeMixIn {}
// mapper.addMixIn(com.some.lib.DangerousType.class, DangerousTypeMixIn.class);
```
⚠ Фильтры `@JsonFilter`/постобработчики, удаляющие типовое поле, **ломают десериализацию** — тестируйте.

# **58.3.5.11. Выводы**

|Практика|Результат|
|---|---|
|Использовать `@JsonTypeInfo(Id.NAME)`|Безопасно|
|Избегать `Id.CLASS` в публичных API|Опасность RCE|
|Использовать `JsonSubTypes`|Явное ограничение типов|
|Настроить `PolymorphicTypeValidator`|Профессиональная защита|
|Добавлять типовое поле вручную|Удобно и прозрачно|


