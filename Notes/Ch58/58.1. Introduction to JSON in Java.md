2025-10-23 11:17
Tags: #JSON #XML #REST #DataBinding #TreeModel  #Streaming #Jackson #JSON-P #JSON-B

`JSON` является основным форматом обмена данными в Java-экосистеме. Он возник как ответ на сложность и избыточность `XML` и стал стандартом в эпоху Web 2.0, AJAX и REST-архитектуры. Сегодня `JSON` используется во всех современных Java-фреймворках (Spring, Jakarta EE, Micronaut, Quarkus), является частью спецификаций OpenAPI, CloudEvents, Kubernetes API и применяется для передачи данных между микросервисами, браузерами, мобильными приложениями и облачными платформами.

`JSON` рассматривается не просто как формат сериализации, а как **контракт взаимодействия** — формальное описание того, какие данные принимает и возвращает система.

# 58.1.1. Формальное определение и структура JSON
**Определение.** `JSON (JavaScript Object Notation)` — текстовый формат обмена данными, основанный на парах «ключ–значение» и упорядоченных списках, определённый в RFC 8259.

### Грамматика JSON (RFC 8259)
```sql
value = false / null / true / object / array / number / string

object = begin-object [ member *( value-separator member ) ] end-object
member = string name-separator value

array  = begin-array [ value *( value-separator value ) ] end-array
```

_Нормативные источники:_ **RFC 8259** и **ECMA-404** (The JSON Data Interchange Syntax).

### Базовые типы

| Тип JSON | Описание                  | Пример                     |
| -------- | ------------------------- | -------------------------- |
| object   | Набор пар «ключ–значение» | `{"id": 1, "name": "Ann"}` |
| array    | Упорядоченный список      | `[1, 2, 3]`                |
| string   | Unicode-строка            | `"hello"`                  |
| number   | Числа                     | `42`, `3.14`               |
| boolean  | Логическое значение       | `true`                     |
| null     | Отсутствие данных         | `null`                     |

## 58.1.1.1. Почему JSON стал стандартом де-факто
Ключевые свойства
- Простая, человекочитаемая структура.
- Универсальность и межъязыковая совместимость.
- Нативная поддержка во фронтенде и backend Java.
- Компактность и эффективность при передаче по сети.

Классические сценарии
- `REST API` и микросервисы.
- Событийные системы и брокеры данных.
- Конфигурации (Kubernetes, Micronaut).
- Хранение и обмен структурированными данными.

Пример `JSON`
```json
{
  "id": "u1",
  "name": "Ann",
  "roles": ["USER", "ADMIN"],
  "active": true
}
```

## 58.1.1.2. Ограничения формата JSON (RFC 8259 / ECMA-404)
- **Комментарии** запрещены (никаких `//` и `/* */`), `trailing comma` тоже запрещена.
- **Числа**: нет `NaN`, `Infinity`, `-Infinity`; только конечные десятичные.
- **Строки** — только в **двойных** кавычках, с Unicode-эскейпами `\uXXXX`.
- **Кодировка**: по умолчанию **UTF-8**; BOM не обязателен и часто нежелателен.
- **Порядок полей** в объекте не считается семантикой JSON; полагаться на него нельзя (если иное не закреплено контрактом).

# 58.1.2. JSON vs XML: сравнение форматов

| Критерий           | JSON                           | XML                                   |
| ------------------ | ------------------------------ | ------------------------------------- |
| Читаемость         | Лаконичный текстовый синтаксис | Много тегов, сложнее визуально        |
| Типы данных        | Поддерживаются примитивы       | Все значения — строки                 |
| Схемы              | Необязательны (JSON Schema)    | Строгие XSD-схемы                     |
| Производительность | Меньший объем данных           | Больше накладных расходов             |
| Комментарии        | Нет (в оригинале)              | Есть                                  |
| Использование      | REST, микросервисы, mobile     | SOAP, банковские стандарты, документы |

**Порядок полей.** `JSON`-спецификация не придаёт ему семантики; если порядок нужен (под подписи/снапшоты), это должно быть **частью контракта** и/или включено настройками сериализации.

**Вывод.** `JSON` — выбор по умолчанию для сервисов и обмена данными в `Java`-мире. `XML` актуален там, где критична схема и требуются контрактные гарантии низкого уровня.

# 58.1.3. JSON в экосистеме Java: стандарты и спецификации (JSR/Jakarta)
`JSON` поддерживается в Java на уровне официальных спецификаций

| Спецификация                               | Название | Назначение               | Тип API                 | Уровень             |
| ------------------------------------------ | -------- | ------------------------ | ----------------------- | ------------------- |
| JSR 353 / Jakarta JSON Processing (JSON-P) | JSON-P   | Streaming и Object Model | Низкоуровневый контроль | Стандарт Jakarta EE |
| JSR 367 / Jakarta JSON Binding (JSON-B)    | JSON-B   | Data Binding (Java↔JSON) | Высокоуровневое API     | Стандарт Jakarta EE |

**Примечание.** `Jackson` формально не входит в `Jakarta EE`, но де-факто является промышленным стандартом `JVM` благодаря производительности и экосистеме. `JSON-P` предоставляет потоковые и объектные API, `JSON-B` поверх него — биндинг. `Jackson` реализует все три подхода (`Streaming/Tree/Data Binding`); `Gson` покрывает `Tree/Data Binding`, но не имеет потокового API.

# 58.1.4. Типичные сценарии использования JSON в Java

| Сценарий            | Описание                                                                          |
| ------------------- | --------------------------------------------------------------------------------- |
| REST и HTTP API     | Передача DTO между клиентом и сервером                                            |
| Микросервисы        | Событийное взаимодействие через Kafka, REST, gRPC JSON mapping / HTTP transcoding |
| Конфигурации        | JSON как формат инфраструктурных настроек                                         |
| Cloud и DevOps      | CloudEvents, Terraform (HCL и JSON-эквивалент), Kubernetes CRD                    |
| Data Lake / Logging | Структурированные логи, аналитика                                                 |

# 58.1.5. JSON как контракт API
`JSON` используется как публичный интерфейс (API) между системой и потребителями. В этом контексте он:
- **Версионируется** (v1, v2).
- Является **контрактом данных** (особенно в микросервисах).
- Требует **обратной совместимости** при изменениях.
- Описывается через `JSON Schema` или `OpenAPI (Swagger)`.

# 58.1.6. Способы работы с JSON в Java (Streaming, Tree, Data Binding)

|Подход|API реализующие|Описание|Когда использовать|
|---|---|---|---|
|Streaming|Jackson Streaming API, JSON-P|Низкоуровневое посимвольное чтение/запись JSON|Большие файлы, потоковая фильтрация|
|Tree Model|Jackson `JsonNode`, Gson `JsonObject`, JSON-P `JsonObject`|JSON как изменяемое дерево|Манипуляция, патчи, неизвестная схема|
|Data Binding|Jackson `ObjectMapper`, JSON-B `Jsonb`, Gson|Прямое преобразование Java ↔ JSON|90–95% прикладных задач|

Правило выбора
• `Data Binding` — по умолчанию для `DTO/REST`.  
• `Tree` — когда структура частично динамическая, нужны правки «на лету», патчи, `JSON Pointer/Path`.  
• `Streaming` — когда объёмы велики, важны O(1) по памяти и минимальные аллокации.

> Практика: `Data Binding` — основной механизм; `Tree` и `Streaming` — для динамических схем и/или производительности.

## 58.1.6.1. Мини-примеры (`Jackson`)
A) `Streaming`: фильтрация без материализации
```java
var f = com.fasterxml.jackson.core.JsonFactory.builder().build();
try (var in = Files.newInputStream(Path.of("big.json"));
     var p  = f.createParser(in);
     var out = Files.newBufferedWriter(Path.of("only-ids.json"), StandardCharsets.UTF_8);
     var g  = f.createGenerator(out)) {

  if (p.nextToken() != JsonToken.START_ARRAY) throw new IllegalStateException();
  g.writeStartArray();
  while (p.nextToken() == JsonToken.START_OBJECT) {
    g.writeStartObject();
    while (p.nextToken() != JsonToken.END_OBJECT) {
      if (p.currentToken() == JsonToken.FIELD_NAME) {
        String name = p.getCurrentName();
        p.nextToken();
        if ("id".equals(name)) { g.writeFieldName("id"); g.copyCurrentEvent(p); }
        else p.skipChildren();
      }
    }
    g.writeEndObject();
  }
  g.writeEndArray();
}
```

B) `Tree Model`: точечная правка
```java
ObjectMapper mapper = new ObjectMapper();
ObjectNode root = (ObjectNode) mapper.readTree("{\"user\":{\"name\":\"Ann\"}}");
root.with("user").put("active", true);
String out = mapper.writeValueAsString(root);
```

C) `Data Binding`: туда-обратно
```java
record User(String id, String name, boolean active) {}

ObjectMapper mapper = new ObjectMapper();
String json = "{\"id\":\"u1\",\"name\":\"Ann\",\"active\":true}";
User u = mapper.readValue(json, User.class);
String back = mapper.writeValueAsString(u);
```

D) `JSON-P`: потоковое чтение
```java
try (var reader = Files.newBufferedReader(Path.of("data.json"), StandardCharsets.UTF_8);
     jakarta.json.stream.JsonParser p = jakarta.json.Json.createParser(reader)) {
  while (p.hasNext()) {
    var e = p.next();
    if (e == jakarta.json.stream.JsonParser.Event.KEY_NAME) {
      String key = p.getString();
      // обработка ключа...
    }
  }
}
```

E) `JSON-B`: биндинг туда-обратно
```java
record UserDto(String id, String name, boolean active) {}

jakarta.json.bind.Jsonb jsonb = jakarta.json.bind.JsonbBuilder.create();
String json = jsonb.toJson(new UserDto("u1", "Ann", true));
UserDto u = jsonb.fromJson(json, UserDto.class);
```

# 58.1.7. Сопоставление типов JSON ↔ Java (по умолчанию)

| JSON    | Java (типично в Jackson)                      | Примечания                                                                                |
| ------- | --------------------------------------------- | ----------------------------------------------------------------------------------------- |
| object  | `POJO/record`, `Map<String, ?>`               | Для records часто не нужны аннотации: имена параметров конструктора совпадают с полями    |
| array   | `List<T>` / `T[]`                             | Для generics используйте `TypeReference`                                                  |
| string  | `String`, `Enum`, `UUID`, `java.time.*`       | Для `java.time` подключайте `jackson-datatype-jsr310` + `JavaTimeModule`, пишите ISO-8601 |
| number  | `int/long/double` или `BigDecimal/BigInteger` | Для точности чисел — `BigDecimal`                                                         |
| boolean | `boolean/Boolean`                             | `Boxing/unboxing`                                                                         |
| null    | `null` (ссылочные)                            | Следите за NPE при анбоксинге                                                             |

# 58.1.8. I/O и кодировки
- Явно указывайте **UTF-8**: `Files.newBufferedReader(path, StandardCharsets.UTF_8)`.
- Избегайте промежуточных `String` для больших объёмов: читайте/пишите **напрямую** из `InputStream`/`OutputStream`.
- Для больших коллекций не формируйте JSON «в строку» — используйте `SequenceWriter/Streaming`.

## 58.1.8.1. Числа и даты: точность и совместимость
- Для денежных/точных величин используйте `BigDecimal` (и **не** записывайте через промежуточную `String` без необходимости).
- Для дат/времени в API **ISO-8601** (например, `2025-10-23T10:15:30Z`) + `jackson-datatype-jsr310`.
- Если потребитель ожидает миллисекунды эпохи — зафиксируйте это в контракте и тестах (`snapshot/golden`).

Пример «без потери точности»
```java
ObjectMapper mapper = new ObjectMapper()
    .registerModule(new com.fasterxml.jackson.datatype.jsr310.JavaTimeModule())
    .disable(com.fasterxml.jackson.databind.SerializationFeature.WRITE_DATES_AS_TIMESTAMPS);
```

# 58.1.9. Частые ошибки (и как избежать)
- Создание `new ObjectMapper()` в каждом методе → **потеря кешей**, лишние аллокации.  
    _Решение:_ `singleton` и/или `ObjectReader`/`ObjectWriter`.    
- Некорректная обработка дат/времени → числа эпохи/ошибки парсинга.  
    _Решение:_ `jackson-datatype-jsr310` + `JavaTimeModule`, ISO-8601.
- «Мягкий» парсинг там, где нужна строгость (числа из строк, одиночное значение как массив).  
    _Решение:_ фиксировать политику coercion/leniency на маппере/ридере.

## 58.1.9.1. Практика I/O для крупных данных
- Читайте/пишите **потоками** (`InputStream`/`OutputStream`) и **не держите** весь JSON строкой в памяти.
- Явно указывайте `StandardCharsets.UTF_8` для `Reader/Writer`.
- Для больших коллекций используйте `SequenceWriter` или стриминговый генератор.

Пример записи напрямую в поток
```java
try (var out = Files.newOutputStream(Path.of("out.json"))) {
  new ObjectMapper().writeValue(out, dto); // без промежуточной String
}
```

## 58.1.9.2. Политика строгости vs. снисходительности
В API желательно фиксировать политику приведения типов:
- запрет `float→int` (во избежание потери точности);
- пустые строки как `null` — только если оговорено;
- одиночное значение как массив — только адресно.

Это не только вопрос `Jackson` (см. 58.3), но и _контрактный аспект API_ — избегайте «магии» на границе сервисов.

## 58.1.9.3. Тестирование: сравнивайте структуру, а не строки
Для юнит-тестов сравнивайте **деревья** (`Jackson JsonNode`) или используйте `JSONAssert (lenient)`.
Сравнение «сырых» строк ломается из-за порядка полей/пробелов.

Мини-пример (дерево)
```java
ObjectMapper m = new ObjectMapper();
var expected = m.readTree(expectedJson);
var actual   = m.readTree(actualJson);
if (!expected.equals(actual)) {
  throw new AssertionError("JSON structures differ");
}
```

# 58.1.10. Переход к библиотекам JSON и мотивация к изучению Jackson
В Java доступны несколько инструментов, реализующих различные уровни работы с JSON. `Jackson` является де-факто стандартом благодаря:
- высокой производительности и богатой экосистеме модулей (`JavaTime`, `Kotlin`, `Afterburner`, `CBOR/Smile` и др.),
- глубокой интеграции со `Spring Boot`, `Micronaut`, `Quarkus`,
- поддержке всех трёх подходов (`Streaming`, `Tree`, `Data Binding`) и тонкой настройке через аннотации/модули.

`Gson` остаётся востребованным для простых/legacy-сценариев. ``JSON-P/JSON-B`` — официальные стандарты Jakarta EE.

➡ В **58.2** — краткий обзор библиотек и критерии выбора.  
➡ В **58.3** — подробный разбор Jackson.

# 58.1.11. Сравнение JSON API в Java (Jackson, Gson, JSON-P, JSON-B)

|Уровень API|Jackson|Gson|Jakarta JSON-P|Jakarta JSON-B|
|---|---|---|---|---|
|Streaming|✅|❌|✅|❌|
|Tree Model|✅|✅|✅|❌|
|Data Binding|✅|✅|❌|✅|
|Производительность|🔥 Высокая|🟡 Средняя|🟡 Средняя|🟡 Средняя|
|Расширяемость|Очень высокая|Средняя|Низкая|Низкая|
|Стандарт Jakarta|❌|❌|✅|✅|

**Итог.** Для промышленной разработки на Java чаще всего выбирают `Jackson`; стандарты `Jakarta` применяют в контейнерных окружениях; Gson подходит для простых задач.
