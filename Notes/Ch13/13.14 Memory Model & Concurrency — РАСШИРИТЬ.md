2025-11-04 13:13
Tags: #


- **Безопасная публикация**: defensive copy наружу; `volatile` замена ссылки (atomic reference replace).
    
- **Иммутабельные паттерны**: final-ссылка + договор о неизменности содержимого.
    
- **JMM-видимость**: когда требуется `synchronized`/`VarHandle`/`Atomic*` для элементов.
    
- **Copy-on-write массивы** на уровне ссылки; избегание false-sharing для примитивов.

- Безопасная публикация (defensive copy, `volatile` замена ссылки).
    
- Иммутабельные паттерны (final-ссылка + договор об неизменности содержимого).
    
- JMM-видимость, когда нужен `synchronized`/`VarHandle`/атомики для элементов.
    
- Copy-on-write массивы как подход (на уровне ссылки).
    