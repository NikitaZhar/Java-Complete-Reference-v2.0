2025-10-29 19:28
Tags: #


Аннотации являются ключевым механизмом управления процессом сериализации и десериализации в Jackson. С их помощью можно точно задать, какие поля включать в JSON, как они называются, какие правила использовать при создании объекта, а также обеспечить поддержку неизменяемых (immutable) классов, полиморфизма и сложных структур данных.

> 🎯 **Цель главы** — сформировать глубокое понимание поведения JSON-сериализации в Java на уровне "внутреннего устройства", а не просто запомнить аннотации.

---

## **58.3.3.1. Категории аннотаций Jackson**

|Категория|Назначение|Основные аннотации|
|---|---|---|
|Управление свойствами|Имя, включение, исключение|`@JsonProperty`, `@JsonIgnore`, `@JsonInclude`|
|Конструкторы / фабрики|Создание объектов|`@JsonCreator`, `@JsonProperty`, `@JacksonInject`|
|Полиморфизм|Включение метаданных о типе|`@JsonTypeInfo`, `@JsonSubTypes`, `@JsonTypeName`|
|Настройка порядка и видимости|Порядок полей, выбор getters/setters|`@JsonPropertyOrder`, `@JsonAutoDetect`|
|Форматирование|Дата, числа|`@JsonFormat`|
|Views (частичный JSON)|Разные представления одного класса|`@JsonView`|

---

# **58.3.3.2. Базовые аннотации управления полями**

### 🔷 `@JsonProperty`

Используется для явного указания имени поля в JSON, даже если имя переменной другое.

```java
class User {
    @JsonProperty("full_name")
    private String name;
}
```

✅ Полезно для:

- интеграции с внешними API
    
- миграции моделей
    
- соответствия стандартам именования (`snake_case`, `camelCase`)
    

### ⚠ Edge case:

Если поле `private` и нет геттера, `@JsonProperty` делает его видимым.

---

### 🔷 `@JsonIgnore`

Полностью исключает поле из сериализации и десериализации.

```java
class User {
    public String name;
    @JsonIgnore
    public String password;
}
```

⛔ Пароль не попадет в JSON ни при каких условиях.

---

### 🔷 `@JsonInclude`

Указывает, когда поле должно включаться.

```java
@JsonInclude(JsonInclude.Include.NON_NULL)
class User {
    public String name;
    public String nickname; // будет исключено, если == null
}
```

Варианты:

- `ALWAYS` (по умолчанию)
    
- `NON_NULL`
    
- `NON_EMPTY`
    
- `NON_DEFAULT`
    

Класс-уровень: игнорировать неизвестные
```java
@JsonIgnoreProperties(ignoreUnknown = true)
class User { /* … */ }
```

Локальная альтернатива глобальному `FAIL_ON_UNKNOWN_PROPERTIES = false`.



---

# **58.3.3.3. Аннотации конструкторов и неизменяемых классов**

### 🔷 `@JsonCreator`

Используется для десериализации неизменяемых объектов (например, `record`, `final class`).

```java
class User {
    private final String name;
    private final int age;

    @JsonCreator
    public User(@JsonProperty("name") String name, @JsonProperty("age") int age) {
        this.name = name;
        this.age = age;
    }
}
```

📌 **Без @JsonCreator десериализация упадёт с `InvalidDefinitionException`.**

---

# **58.3.3.4. Полиморфная сериализация**

🚨 JSON по умолчанию не знает тип объекта. Для сериализации интерфейсов или наследников нужно явно включить информацию о типе.

```java
@JsonTypeInfo(
  use = JsonTypeInfo.Id.CLASS,
  include = JsonTypeInfo.As.PROPERTY,
  property = "@class")
class Animal {}

class Dog extends Animal {
    public String name;
}
```

JSON:

```json
{ "@class": "com.example.Dog", "name": "Rex" }
```

⚠ **Без этого Jackson не сможет создать нужный подкласс при десериализации.**

⚠ **Никогда не используйте `JsonTypeInfo.Id.CLASS` в публичных API.** Для безопасности см. 58.3.5.



# **58.3.3.5. @JsonFormat — форматирование**

```java
class Event {
    @JsonFormat(shape = JsonFormat.Shape.STRING, pattern = "yyyy-MM-dd HH:mm")
    public LocalDateTime date;
}
```

➡ Управляет форматами JSON → Java, не требует кастомных сериализаторов.

Пример для чисел/локали и таймзоны:
```java
class Invoice {
  @JsonFormat(shape = JsonFormat.Shape.STRING, pattern = "#,##0.00", locale = "en_US")
  public BigDecimal amount;

  @JsonFormat(shape = JsonFormat.Shape.STRING, pattern = "yyyy-MM-dd'T'HH:mm:ssXXX", timezone = "UTC")
  public OffsetDateTime issuedAt;
}
```

---

# **58.3.3.6. Частичные представления JSON – @JsonView**

Позволяет выводить одно и то же поле по-разному для разных потребителей.

```java
class User {
    @JsonView(Views.Public.class)
    public String name;

    @JsonView(Views.Internal.class)
    public String password;
}
```

Использование:

```java
mapper.writerWithView(Views.Public.class).writeValueAsString(user);
```


- **Циклы в графах объектов**

```java
class User { @JsonManagedReference public List<Order> orders; } 
class Order { @JsonBackReference public User user; }
```

Это разрывает цикл «родитель→дети→родитель».  
Альтернатива — **идентификаторы**:

```java
@JsonIdentityInfo(generator = ObjectIdGenerators.PropertyGenerator.class, property = "id") 
class User { Long id; List<Order> orders; }
```

`@JsonIdentityInfo` удобнее при сложных графах, но требует уникальных идентификаторов.

---

# **58.3.3.7. Ошибки и edge cases**

### ❌ Конфликт имен

```java
class Test {
   @JsonProperty("value")
   public String a;
   @JsonProperty("value")
   public String b; // Ошибка: дублирование JSON-поля
}
```

### ❌ Игнорирование final поля

Если поле `final` без @JsonCreator — значение не будет установлено, и Jackson выбросит ошибку.

### ❌ @JsonIgnore и @JsonProperty вместе

```java
@JsonIgnore
@JsonProperty("id")
public int id;
```

➡ `@JsonProperty` имеет более высокий приоритет → поле будет включено.

---



## **58.3.3.9. Аннотации для расширенной модели свойств**

Ниже — аннотации, которые дают тонкий контроль над тем, **что считать свойством**, **как его называть**, **как заполнять отсутствующие/`null` значения**, **куда складывать «лишние» поля**, как **расплющивать вложенные объекты** и как **гарантировать стабильный порядок** и **видимость**.

### **`@JsonSetter` / `@JsonGetter` (+ политика `nulls`)**

Используются для явного указания сеттера/геттера и поведения при `null`.

```java
class User {
    private String name;
    private List<String> tags = new ArrayList<>();

    @JsonGetter("full_name")
    public String getName() { return name; }

    @JsonSetter(value = "full_name", nulls = Nulls.FAIL)
    public void setName(String name) { this.name = name; }

    // nulls=SKIP не заменит коллекцию на null и не очистит её
    @JsonSetter(value = "tags", nulls = Nulls.SKIP, contentNulls = Nulls.SKIP)
    public void setTags(List<String> tags) {
        if (tags != null) this.tags = tags;
    }
}
```

- `nulls = Nulls.FAIL|SET|SKIP` — политика для самого свойства.
    
- `contentNulls = …` — политика для **элементов коллекций/карт**.
    
- Частый кейс: запретить «обнуление» коллекций входящим `null`.
    

---

### **`@JsonAlias` — альтернативные имена полей**

Позволяет принимать несколько имён на вход (миграции API, обратная совместимость).

```java
class User {
    @JsonAlias({"fullName", "full_name"})
    @JsonProperty("name")
    public String name;
}
```

- Сериализация будет как `"name"`, а десериализация примет и `"fullName"`, и `"full_name"`.
    

---

### **`@JsonAnySetter` / `@JsonAnyGetter` — «пылесос» для лишних полей**

Собирают и отдают свойства, **не описанные** в модели.

```java
class Extensible {
    public String id;

    private final Map<String, Object> extra = new LinkedHashMap<>();

    @JsonAnySetter
    void put(String key, Object value) { extra.put(key, value); }

    @JsonAnyGetter
    Map<String, Object> any() { return extra; }
}
```

- Полезно для **форвард-совместимости**: новые поля не потеряются.
    
- Внимание: контролируйте объём `extra`, чтобы не схлопотать OOM.
    

---

### **`@JsonUnwrapped` — расплющивание вложенного объекта**

Встраивает поля вложенного объекта в уровень-родителя.

```java
class Address { public String city; public String street; }

class User {
    public String name;

    @JsonUnwrapped
    public Address address;
}
```

JSON:

```json
{ "name":"Alice", "city":"Berlin", "street":"Unter den Linden" }
```

- ⚠ Возможны **коллизии имён**. Для префиксов/суффиксов используйте  
    `@JsonUnwrapped(prefix="addr_", suffix="_val")`.
    

---

### **`@JsonPropertyOrder` — стабильный порядок полей**

Делает вывод **детерминированным** (полезно для снапшот-тестов).

```java
@JsonPropertyOrder({ "id", "name", "createdAt" })
class User {
    public String name;
    public String id;
    public Instant createdAt;
}
```

- Можно дополнить глобально: `SerializationFeature.ORDER_MAP_ENTRIES_BY_KEYS`.
    

---

### **`@JsonAutoDetect` — управление видимостью**

Когда нужно сериализовать **поля напрямую** или, наоборот, **игнорировать поля** и использовать только геттеры/сеттеры.

```java
@JsonAutoDetect(fieldVisibility = JsonAutoDetect.Visibility.ANY,
                getterVisibility = JsonAutoDetect.Visibility.NONE,
                isGetterVisibility = JsonAutoDetect.Visibility.NONE,
                setterVisibility = JsonAutoDetect.Visibility.NONE)
class RawDTO {
    String a; // будет виден как поле
    private String b; // тоже будет виден
}
```

- Удобно для простых DTO без геттеров.
    
- ⚠ Внимание к приватным данным: легко «засветить» лишнее.
    

---

#### Частые ошибки и тонкости

- `@JsonSetter(nulls = Nulls.SKIP)` не вызовет метод при `null` — коллекции не обнулятся.
    
- `@JsonUnwrapped` на `null`-поле ничего не добавит — учитывайте в контрактах.
    
- `@JsonAnySetter` может «съесть» опечатки в полях — логируйте ключи или валидируйте.
    

---

## **58.3.3.10. Иммутабельные классы: builders и records**

Иммутабельные модели — стандарт в продакшене. Jackson поддерживает их через builder-паттерн и **Java records**.

### **Builder: `@JsonDeserialize(builder = …)` + `@JsonPOJOBuilder`**

Для классов без сеттеров.

```java
@JsonDeserialize(builder = Money.Builder.class)
class Money {
    private final String currency;
    private final BigDecimal amount;

    private Money(Builder b) {
        this.currency = b.currency;
        this.amount = b.amount;
    }

    @JsonPOJOBuilder(withPrefix = "") // важный момент: методы builder'а без префикса set*
    public static class Builder {
        private String currency;
        private BigDecimal amount;

        public Builder currency(String c) { this.currency = c; return this; }
        public Builder amount(BigDecimal a) { this.amount = a; return this; }

        public Money build() { return new Money(this); }
    }
}
```

- Если методы называются `setXxx`, используйте `withPrefix = "set"`.
    
- Можно добавлять `@JsonProperty` на методы билдера для переименования входов.
    

#### **Lombok (`@Builder`, `@Value`) — ловушки**

Lombok создаёт `ClassNameBuilder` с методами **без префикса**, поэтому:

```java
@JsonDeserialize(builder = Order.OrderBuilder.class)
@lombok.Value
@lombok.Builder
class Order {
    String id; Instant createdAt;

    @JsonPOJOBuilder(withPrefix = "") // требуется почти всегда
    public static class OrderBuilder {}
}
```

- Частая ошибка: забыли `@JsonPOJOBuilder(withPrefix = "")` → `InvalidDefinitionException`.
    
- Для полей-алиасов используйте `@JsonAlias` **на параметрах билдера** или `@JsonProperty` на полях класса.
    

---

### **Java records**

Records — иммутабельные по определению. Jackson поддерживает их «из коробки», **если** доступны имена параметров конструктора.

```java
public record User(String name, int age) {}
```

Достаточно одного из:

1. Компиляция с `-parameters` **или**
    
2. Подключён `ParameterNamesModule` **или**
    
3. Аннотировать конструктор `@JsonCreator` и компоненты `@JsonProperty`.
    

```java
// Вариант с аннотациями (без -parameters)
public record User(
    @JsonProperty("name") String name,
    @JsonProperty("age") int age) {

    @JsonCreator
    public User {}
}
```

- Если поля в JSON отличаются по именам — используйте `@JsonProperty` на компонентах record.
    
- Для опциональных значений используйте `Optional<T>` или дефолты через «канонический компактный конструктор».
    

#### Частые ошибки

- Нет `-parameters` и нет `ParameterNamesModule` → Jackson не знает имена параметров record.
    
- Lombok `@Builder` поверх record — потребует явного `@JsonDeserialize(builder=...)` (чаще не нужен; лучше оставить чистый record).
    

---

## **58.3.3.11. Enum и форматы**

Enum по умолчанию сериализуется именем константы (`"SILVER"`). Jackson позволяет переопределять формат.

### **`@JsonValue` — своё строковое/числовое представление**

```java
enum Tier {
    SILVER("silver"), GOLD("gold");
    private final String id;
    Tier(String id) { this.id = id; }

    @JsonValue
    public String json() { return id; }
}
```

JSON: `"silver"`

- Работает и с числами (например, код статуса).
    

### **`@JsonCreator` — обратное преобразование**

```java
enum Tier {
    SILVER("silver"), GOLD("gold");
    private final String id;

    Tier(String id) { this.id = id; }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static Tier from(String v) {
        for (var t : values()) if (t.id.equalsIgnoreCase(v)) return t;
        throw new IllegalArgumentException("Unknown tier: " + v);
    }
}
```

- `Mode.DELEGATING` — принимает **одно** простое значение (`String/number`) и мапит на enum.
    

---

### **Глобальные флаги для enum**

Включаются на `ObjectMapper`:

```java
mapper.enable(SerializationFeature.WRITE_ENUMS_USING_TO_STRING);
mapper.enable(DeserializationFeature.READ_ENUMS_USING_TO_STRING);

mapper.enable(DeserializationFeature.READ_UNKNOWN_ENUM_VALUES_AS_NULL);
// или:
mapper.enable(DeserializationFeature.READ_UNKNOWN_ENUM_VALUES_USING_DEFAULT_VALUE);
```

Пояснения:

- **`WRITE_ENUMS_USING_TO_STRING`** — сериализация через `Enum.toString()` (часто вместе с переопределённым `toString()`).
    
- **`READ_ENUMS_USING_TO_STRING`** — обратная сторона: парсинг по `toString()`.
    
- **`READ_UNKNOWN_ENUM_VALUES_AS_NULL`** — неизвестное значение → `null` (аккуратно с примитивами).
    
- **`READ_UNKNOWN_ENUM_VALUES_USING_DEFAULT_VALUE`** — мапит на `@JsonEnumDefaultValue`.
    

```java
enum Color {
    RED, GREEN,
    @JsonEnumDefaultValue UNKNOWN
}
```

Теперь `"BLUE"` → `UNKNOWN`.

---

### **Типичные ошибки и рекомендации**

- Одновременное использование `@JsonValue` и `WRITE_ENUMS_USING_TO_STRING` может запутать: выберите один механизм.
    
- Для публичных API используйте **стабильные строковые коды** (через `@JsonValue`), а не имена констант — так проще версионировать.
    
- При включённом `READ_UNKNOWN_ENUM_VALUES_AS_NULL` проверяйте `null` и валидируйте — иначе можно «проглотить» критическую ошибку схемы.
    

---

### **Короткие best practices (для всего подпункта 58.3.3.9–11)**

- Явно задавайте политику `nulls` для коллекций и карт (`@JsonSetter(contentNulls=…)`).
    
- Для иммутабельных моделей используйте **records** там, где это возможно; для сложных — **builder** с `@JsonPOJOBuilder(withPrefix="")`.
    
- Для enum зафиксируйте строковый контракт через `@JsonValue` и обработчик через `@JsonCreator`.
    
- Избегайте «магии» — лучше **явно** указывать имена (`@JsonProperty`) в граничных контрактах.

## **58.3.3.8. Выводы**

- Аннотации — основной интерфейс управления поведением Jackson.
    
- `@JsonCreator` обязателен для immutable классов.
    
- Неправильное использование полиморфизма может привести к RCE-уязвимостям.
    
- `@JsonInclude` и `@JsonIgnore` — инструменты оптимизации размера JSON.
    
- `@JsonView` — механизм контроля уровня доступа к данным.

