2025-10-29 19:40
Tags: #


_(центральная тема практического использования Jackson в реальных системах: от REST API до микросервисов и high-performance инженерии)_

**ObjectMapper** — мощный инструмент, но по умолчанию он настроен на совместимость, а не на строгую сериализацию или производительность. Правильная конфигурация позволяет:

- контролировать формат JSON,
    
- обеспечивать стабильность API,
    
- включать/выключать функциональность,
    
- предотвращать ошибки и уязвимости,
    
- повышать производительность.
    

> 🎯 _В этом разделе рассматриваются все ключевые настройки, жизненно важные для промышленного кода._

---

# **58.3.8.1. Основные направления настройки**

|Направление|Что регулируется|
|---|---|
|Сериализация|Как Java-объект превращается в JSON|
|Десериализация|Как JSON превращается в Java-объект|
|Исключения|Как Jackson реагирует на ошибки|
|Включение/исключение полей|`null`, пустые коллекции, значения по умолчанию|
|Типизация|Полиморфизм, безопасность|
|Производительность|Кеш, байткод-генераторы (Afterburner)|
|Naming Strategy|snake_case, camelCase и др.|

---

# **58.3.8.2. Базовые опции сериализации (SerializationFeature)**

```java
mapper.enable(SerializationFeature.INDENT_OUTPUT); // Читабельный (pretty) JSON
mapper.disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS); // Формат дат как строки ISO
mapper.enable(SerializationFeature.ORDER_MAP_ENTRIES_BY_KEYS); // Стабильный порядок
mapper.disable(SerializationFeature.FAIL_ON_EMPTY_BEANS); // Не выбрасывать ошибку, если нет геттеров
```

### ✅ Рекомендуемые настройки для REST API:

```java
mapper
  .disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS)
  .enable(SerializationFeature.INDENT_OUTPUT)
  .setSerializationInclusion(JsonInclude.Include.NON_NULL);
```

---

# **58.3.8.3. Настройки десериализации (DeserializationFeature)**

```java
mapper.disable(DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES); // Игнорировать лишние поля
mapper.enable(DeserializationFeature.ACCEPT_SINGLE_VALUE_AS_ARRAY); // Принять "value" вместо ["value"]
mapper.enable(DeserializationFeature.FAIL_ON_NULL_FOR_PRIMITIVES); // Null → ошибка, если поле primitive
mapper.disable(DeserializationFeature.FAIL_ON_IGNORED_PROPERTIES); // Ошибки при игнорируемых полях
```

### ⚠ Опасность:

`FAIL_ON_UNKNOWN_PROPERTIES = false` упрощает разработку, но может скрыть изменение схемы данных.


#### **58.3.8.3a. Строгая десериализация и удобные допуски**

```java
// Строгий хвост: запрет мусора после валидного JSON
mapper.enable(DeserializationFeature.FAIL_ON_TRAILING_TOKENS);

// Регистр повыш/пониж в именах/enum
mapper.enable(MapperFeature.ACCEPT_CASE_INSENSITIVE_PROPERTIES);
mapper.enable(MapperFeature.ACCEPT_CASE_INSENSITIVE_ENUMS);

// Числа для enum только строками
mapper.enable(DeserializationFeature.FAIL_ON_NUMBERS_FOR_ENUMS);

// Пустая строка как null-объект (осторожно)
mapper.enable(DeserializationFeature.ACCEPT_EMPTY_STRING_AS_NULL_OBJECT);

// Дубликаты ключей — фатальная ошибка (Tree)
mapper.enable(DeserializationFeature.FAIL_ON_READING_DUP_TREE_KEY);

// Для парсера уровня core (если управляете фабрикой):
mapper.getFactory().enable(JsonParser.Feature.STRICT_DUPLICATE_DETECTION);
```

CoercionConfig — тонкий контроль преобразований
```java
var cfg = mapper.coercionConfigFor(LogicalType.Integer);
cfg.setCoercion(CoercionInputShape.EmptyString, CoercionAction.Fail);
mapper.coercionConfigFor(LogicalType.Boolean)
      .setCoercion(CoercionInputShape.EmptyString, CoercionAction.Fail);
```



# **58.3.8.4. Naming Strategies (стратегии именования полей)**

Jackson поддерживает автоматическое преобразование имён:

```java
mapper.setPropertyNamingStrategy(PropertyNamingStrategies.SNAKE_CASE);
```

|Стратегия|Пример|
|---|---|
|SNAKE_CASE|`user_name`|
|UPPER_CAMEL_CASE|`UserName`|
|LOWER_CAMEL_CASE|`userName` _(по умолчанию)_|
|KEBAB_CASE|`user-name`|

---

# **58.3.8.5. Включение / исключение полей**

### Глобально:

```java
mapper.setSerializationInclusion(JsonInclude.Include.NON_NULL);
```

### На уровне класса:

```java
@JsonInclude(JsonInclude.Include.NON_EMPTY)
class User {
    String name;
    List<String> roles; // если пусто → не будет включено
}
```

Виды:

- `NON_NULL` — исключает null
    
- `NON_EMPTY` — исключает пустые коллекции
    
- `NON_DEFAULT` — исключает значения по умолчанию
    

---

# **58.3.8.6. Работа с датами и временем**

```java
mapper.registerModule(new JavaTimeModule());
mapper.disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS); // ISO-8601
mapper.enable(DeserializationFeature.READ_DATE_TIMESTAMPS_AS_NANOSECONDS);
```

Пример вывода:

```json
"2025-10-24T10:15:30"
```

---

# **58.3.8.7. Кастомные сериализаторы и десериализаторы**

```java
public class MoneySerializer extends JsonSerializer<BigDecimal> {
    @Override
    public void serialize(BigDecimal value, JsonGenerator gen, SerializerProvider serializers) throws IOException {
        gen.writeString("$" + value.toString());
    }
}

SimpleModule module = new SimpleModule();
module.addSerializer(BigDecimal.class, new MoneySerializer());
mapper.registerModule(module);
```

---

# **58.3.8.8. Mixin-аннотации (добавление аннотаций без изменения кода)**

```java
abstract class UserMixin {
    @JsonIgnore
    String password;
}

mapper.addMixIn(User.class, UserMixin.class);
```

✅ Используется для сериализации сторонних библиотек или immutable-классов.

#### **58.3.8.x Политики `null`/пустых значений (глобально)**

```java
mapper.setDefaultPropertyInclusion(
  JsonInclude.Value.construct(JsonInclude.Include.NON_NULL,
                              JsonInclude.Include.NON_NULL));
// или короче:
mapper.setSerializationInclusion(JsonInclude.Include.NON_EMPTY);
```

И на уровне полей/коллекций:

```java
class Dto {
  @JsonSetter(nulls = Nulls.SKIP, contentNulls = Nulls.SKIP)
  List<String> tags;
  @JsonInclude(value = JsonInclude.Include.NON_EMPTY, content = JsonInclude.Include.NON_EMPTY)
  Map<String, String> meta;
}
```

#### **58.3.8.y Числа и точность (финансы/наука)**

```java
mapper.enable(DeserializationFeature.USE_BIG_DECIMAL_FOR_FLOATS);
mapper.enable(DeserializationFeature.USE_BIG_INTEGER_FOR_INTS);
mapper.enable(SerializationFeature.WRITE_BIGDECIMAL_AS_PLAIN);
```

#### **58.3.8.z Даты и часовые пояса**

```java
mapper.registerModule(new JavaTimeModule());
mapper.disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS); // ISO-8601
mapper.setTimeZone(TimeZone.getTimeZone("UTC"));
```

`Instant` — всегда UTC; `OffsetDateTime` хранит смещение; `ZonedDateTime` хранит таймзону (например, `Europe/Bratislava`). Выбирайте осознанно.

#### **58.3.8.k Ключи Map не-String**

JSON допускает только строковые ключи:

- Либо сериализуйте даты/enum в строку через **key сериализатор**:
    

```java
public class LocalDateKeySer extends JsonSerializer<LocalDate> {
  @Override public void serialize(LocalDate v, JsonGenerator gen, SerializerProvider sp) throws IOException {
    gen.writeFieldName(v.toString());
  }
}
SimpleModule m = new SimpleModule();
m.addKeySerializer(LocalDate.class, new LocalDateKeySer());
mapper.registerModule(m);
```

- И соответствующий **key десериализатор** для обратного преобразования.
    





# **58.3.8.9. Ошибки и исключения**

|Ситуация|Настройка|
|---|---|
|JSON содержит неизвестные поля|`FAIL_ON_UNKNOWN_PROPERTIES`|
|JSON не может быть преобразован|`FAIL_ON_INVALID_SUBTYPE`|
|Поле primitive имеет null|`FAIL_ON_NULL_FOR_PRIMITIVES`|
|Алиасы и несовпадающие имена|`mapper.addMixIn(...)` / `@JsonAlias`|

```java
try {
    mapper.readValue(json, User.class);
} catch (JsonProcessingException e) {
    // Log + реакция
}
```

---

# **58.3.8.10. Лучшие практики**

|Рекомендация|Причина|
|---|---|
|Использовать Singleton ObjectMapper|производительность + кеш|
|Явно регистрировать модули|контроль поведения|
|Отключить timestamp для дат|читаемость и совместимость|
|Включить `FAIL_ON_UNKNOWN_PROPERTIES` на этапе разработки|обнаружение ошибок схемы|
|Использовать MixIn для сторонних библиотек|гибкость|
|Не использовать ObjectMapper как "black box"|важна конфигурация|

---

# ✅ **Выводы**

- ObjectMapper — гибкий и мощный компонент, который необходимо **обязательно конфигурировать**.
    
- Неправильная конфигурация приводит к ошибкам, потере совместимости, проблемам безопасности.
    
- Большинство проблем при работе с JSON в Java **связаны не с Jackson, а с неправильной конфигурацией ObjectMapper**.
    
