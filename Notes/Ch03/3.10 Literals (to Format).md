2025-10-08 16:23
Tags: #Literals #toFormat 

3.10.1 Integer Literals
3.10.2 Floating-Point Literals
3.10.3 Boolean Literals
3.10.4 Character Literals
3.10.5 String Literals
3.10.6 Text Blocks
3.10.7 The Null Literal 


**Литерал** — фиксированное значение, записанное непосредственно в исходном коде программы.
Литералы не вычисляются во время выполнения: они представляют **константные значения**, определяемые компилятором на этапе анализа исходного текста.
Они формируют **лексемы (tokens)** и могут участвовать в **константных выражениях (§15.28)**.

> 💡 Литералы — фундамент синтаксиса Java. Они задают, как конкретные значения (числа, символы, строки, логические константы и `null`) существуют в коде и в памяти.

| Категория          | Тип               | Пример                         |
| :----------------- | :---------------- | :----------------------------- |
| Целочисленные      | `int`, `long`     | `42`, `0xFF`, `0b1010`, `123L` |
| С плавающей точкой | `float`, `double` | `3.14`, `6.022e23`, `0x1.0p4f` |
| Булевы             | `boolean`         | `true`, `false`                |
| Символьные         | `char`            | `'A'`, `'\n'`, `'\u03A9'`      |
| Строковые          | `String`          | `"Hello"`, `"Java\n17"`        |
| Null-литерал       | —                 | `null`                         |

## 3.10.0 Лексическая стадия и Unicode Translation
Перед тем как компилятор анализирует литералы, исходный код проходит несколько фаз.

1. **Unicode translation (§3.2):**
Все символы исходного текста переводятся в Unicode.
Последовательности `\uXXXX` интерпретируются **до** лексического анализа и даже **до обработки комментариев**.

Это может “вскрывать” комментарии, если `escape` создаёт `*/`.

```java
/*\u002a/ comment */
```

В этом примере `\u002a` → `*`, и последовательность превращается в `/**/`, что преждевременно закрывает комментарий.

2. **Lexical analysis (§3.3)**
Поток символов разбивается на токены (идентификаторы, литералы, операторы).

3. **Tokenization и Parsing**
Токены группируются в выражения и структуры кода.
> Понимание этой последовательности критично: `Unicode escapes` могут изменять структуру кода до фазы токенизации.

## 3.10.1 Integer Literals
### Определение
Целочисленные литералы представляют значения типов `int` и `long`.
Тип по умолчанию — `int`; суффикс `L` или `l` создает литерал типа `long`.

```java
int x = 42;
long y = 42L;
```

Отрицательные значения формируются **оператором** `-`, применённым к положительному литералу.

```java
long n = -9223372036854775808L; // корректно, хотя без минуса — ошибка
```

### Системы счисления и устаревшие форматы

| Система               | Префикс     | Пример                | Комментарий       |
| :-------------------- | :---------- | :-------------------- | :---------------- |
| Десятичная            | —           | `42`                  | стандартная форма |
| Шестнадцатеричная     | `0x` / `0X` | `0x2A`, `0xCAFE_BABE` | цифры 0–9, A–F    |
| Двоичная              | `0b` / `0B` | `0b101010`            | с Java 7          |
| Восьмеричная (устар.) | `0`         | `052`                 | до Java 7         |

> До Java 7 запись `010` означала восьмеричное число (8).
> В современных версиях литералы с ведущими нулями (`0010`) трактуются как десятичные.

### Разделители и диапазоны
```java
int million = 1_000_000;
long big = 9_223_372_036_854_775_807L;
```

| Тип    |                    Минимум |                  Максимум | Размер  |
| :----- | -------------------------: | ------------------------: | :------ |
| `int`  |             -2 147 483 648 |             2 147 483 647 | 32 бита |
| `long` | -9 223 372 036 854 775 808 | 9 223 372 036 854 775 807 | 64 бита |

### Narrowing conversions и Constant folding
```java
final int K = 10;
byte b = K; // допустимо
```

> 💡 Если константа помещается в диапазон меньшего типа, приведение выполняется автоматически.

Компилятор вычисляет выражения из литералов на этапе компиляции.

```java
final int A = 2 * 3 * 7;// 42
final String S = "Java " + "17"; // "Java 17"
```

### Numeric promotion и знаковая арифметика

```java
System.out.println(1 / 2); // 0
System.out.println(1 / 2.0); // 0.5
```

Арифметика между литералами следует правилу _`binary numeric promotion` (§5.6.1)_.
В Java 8 появились методы для **беззнаковой арифметики**.

```java
int x = -1;
System.out.println(Integer.toUnsignedString(x)); // 4294967295
System.out.println(Integer.divideUnsigned(-2, 2)); // 2147483647
```

И оператор `>>>` (`zero-fill right shift`).

```java
int a = -8 >>> 1; // 2147483644
```

### Integer.decode и parseInt

Метод `Integer.decode()` интерпретирует строки с теми же префиксами, что и литералы.

```java
int a = Integer.decode("0x10"); // 16
int b = Integer.decode("010");// 8
int c = Integer.decode("#20");// 32
```

> Это API-аналог лексической семантики числовых литералов.

### Переполнение
Переполнение не вызывает исключений — результат берётся по модулю `2³²` или `2⁶⁴`.

```java
int x = 2_000_000_000;
int y = x * 2; // отрицательное значение
```

## 3.10.2 Floating-Point Literals
### Формы записи

```java
double pi = 3.1415;
float e = 2.71828f;
double hex = 0x1.0p4; // 16.0
```

### Hexadecimal Floating-Point Literals
Java поддерживает шестнадцатеричные литералы для `float/double` (§3.10.2).

```java
double v = 0x1.0p-3; // 0.125
```

Формат

```java
0x<significand>p<exponent>
```

> 💡 Это обеспечивает точное управление битовым представлением числа и часто используется в тестах IEEE 754.

### IEEE 754 и signed zeros

```java
double pz = +0.0;
double nz = -0.0;
System.out.println(1.0 / pz); // Infinity
System.out.println(1.0 / nz); // -Infinity
```

### Потеря точности

```java
System.out.println(0.1 + 0.2 == 0.3); // false
```

Используйте `BigDecimal` для точных финансовых вычислений.

## 3.10.3 Boolean Literals
`true` и `false` — единственные булевы литералы.

```java
boolean ready = true;
```

### Autoboxing и Boolean.valueOf
При упаковке используется `Boolean.valueOf()`, возвращающая константы `Boolean.TRUE` / `Boolean.FALSE`

```java
Boolean a = true; // autoboxing
System.out.println(a == Boolean.TRUE); // true
```

### Boolean и constant expressions
Булевы литералы могут участвовать в константных выражениях.

```java
final boolean FLAG = false;
if (FLAG) { ... }
```

> ⚙️ Различие `boolean` и `Boolean` особенно важно при сравнении ключей в коллекциях:
> `Map<Boolean, String> map = Map.of(true, "yes");`

## 3.10.4 Character Literals
### Определение
`char` — 16-битный **кодовый элемент UTF-16**, а не “символ `Unicode`”.

```java
char c = 'A';
char omega = '\u03A9';
```

### Escape-последовательности

|Последовательность|Значение|
|:--|:--|
|`\n`|новая строка|
|`\t`|табуляция|
|`\b`|забой|
|`\r`|возврат каретки|
|`\f`|переход страницы|
|`\\`|обратная косая|
|`\'`|одинарная кавычка|
|`\"`|двойная кавычка|
|`\ddd`|октальный код (0–7, диапазон \000–\377)|
|`\uXXXX`|Unicode-код (hex)|

### Unicode и code points
Java `char` хранит один 16-битный кодовый элемент, но Unicode-символы выше `U+FFFF` требуют пары `char` (`surrogates`).

```java
String s = "😀";
System.out.println(s.length()); // 2
System.out.println(s.codePointCount(0, s.length())); // 1
```

> 💡 `String.length()` ≠ количество символов `Unicode`. Используйте `codePointCount()`.

## 3.10.5 String Literals
### Определение
Строка — неизменяемая последовательность `char`, заключённая в кавычки.

### Escape и нормализация CRLF
Все переводы строк (`\r`, `\r\n`, `\n`) нормализуются к `\n`.

> Это обеспечивает переносимость исходников между ОС.

### Interning
Все строковые литералы интернируются в `String Pool`.

```java
String a = "Java";
String b = "Java";
System.out.println(a == b); // true
```

### Compile-time concatenation
Компилятор объединяет строковые литералы во время компиляции.

```java
String s = "Hello, "
 "world!";
```

### Text Blocks

```java
String html = """
<html>
<body>Hello</body>
</html>
""";
```

#### Escape `\s` и завершающий перевод
- `\s` обозначает пробел;
- пробелы перед закрывающими `"""` игнорируются;
- `\` в конце строки удаляет перевод строки;
- если после `"""` есть перенос, он **не включается** в строку.

## 3.10.6 The `null` Literal
`null` обозначает отсутствие ссылки и совместим со всеми ссылочными типами.

```java
String s = null;
List<Integer> list = null;
```

### Null type
> 💡 `null` имеет собственный “`null type`” (§4.1) — тип без имени, совместимый со всеми ссылочными типами, но не с примитивами.

### Пример с `generics`

```java
class Box<T> { T value = null; }
Box<String> b = new Box<>();
```

### Pattern Matching
`null instanceof T` — всегда `false`.

## 3.10.7 Constant Expressions
Литералы `NaN`, `Infinity`, `true`, `false`, `null` и строковые значения могут участвовать в `compile-time` выражениях (§15.28).

```java
final String MSG = "Ready";
final double MAX = Double.POSITIVE_INFINITY;
```

## 3.10.8 JVM Representation
### Литералы в `constant pool` и пример байткода
При компиляции литералы сохраняются в `constant pool` `.class`-файла.

|Тип литерала|Константа|Формат|
|:--|:--|:--|
|`int`, `float`|`CONSTANT_Integer`, `CONSTANT_Float`|4 байта|
|`long`, `double`|`CONSTANT_Long`, `CONSTANT_Double`|8 байт|
|`String`|`CONSTANT_String` → `CONSTANT_Utf8`|UTF-8|
|`null`|представляется как `aconst_null`|без значения|

Пример

```java
String s = "Hi";
int x = 42;
Object n = null;
```

Байт-код

```java
ldc #2// String "Hi"
bipush 42
aconst_null
```

Constant Pool

```java
#2 = String "Hi"
#3 = Integer 42
```

## 3.10.9 Исторические изменения

|Версия|Изменение|JEP / источник|
|:--|:--|:--|
|Java 5|autoboxing, аннотации, compile-time constants|—|
|Java 7|двоичные литералы (`0b`), `_` в числах|—|
|Java 13|text blocks (preview)|JEP 355|
|Java 15|text blocks (final)|JEP 378|
|Java 16|pattern matching for `instanceof`|JEP 394|
|Java 21|string templates (preview)|JEP 430|

## 📘 Ссылки
- **JLS §3.2–§3.10** — Unicode Translation, Literals
- **JLS §4.1** — The null Type
- **JLS §4.2.3** — Floating-Point Types, Values, and Operations
- **JLS §5.6.1** — Binary Numeric Promotion
- **JLS §9.7.1** — Element Values in Annotations
- **JLS §15.28** — Constant Expressions
- **JEP 355, JEP 378, JEP 394, JEP 430**

