2025-09-15 14:03
Tags: #Unicode #encoding #escape #formfeed #char #BOM
- Поддержка `Unicode` в исходниках
- `Unicode escape`-последовательности  
- BOM и `line terminators (LF, CRLF, CR)`

`Java` изначально проектировалась как язык с поддержкой полного `Unicode`. Это обеспечивает переносимость исходного кода между платформами и корректное отображение любых символов.

Спецификация языка (`JLS §3.1–3.5`) чётко определяет, как исходные файлы должны интерпретироваться с точки зрения кодировок и Unicode.
## Кодировка исходных файлов
- Компилятор `javac` ожидает, что исходный файл представлен в кодировке, совместимой с `Unicode`.
- До `Java 18` кодировка по умолчанию зависела от системных настроек платформы. Это приводило к проблемам при переносе кода между системами.  
- Начиная с `Java 18`, по умолчанию используется `UTF-8` (см. JEP 400).  

Для явного указания кодировки при компиляции применяется ключ:
```bash
javac -encoding UTF-8 MyClass.java
````

Если исходный файл сохранён в другой кодировке, но при компиляции не указано `-encoding`, компилятор может интерпретировать символы неправильно:

❌ Пример ошибки (файл сохранён в `Windows-1251`, компилятор ожидает `UTF-8`)
```java
public class BadEncoding {
    public static void main(String[] args) {
        System.out.println("Привет"); // может превратиться в "РџСЂРёРІРµС‚"
    }
}
```

## Unicode escape-последовательности
Любой символ `Unicode` можно записать как `\uXXXX`, где `XXXX` — четыре шестнадцатеричные цифры.
- Обработка `escape`-последовательностей выполняется ещё до лексического анализа.
- Это означает, что они могут встречаться даже в комментариях или строках, изменяя структуру исходного кода.

✅ Пример корректного использования
```java
public class UnicodeChar {
    public static void main(String[] args) {
        char c = '\u0416'; // символ 'Ж'
        System.out.println(c);
    }
}
```
⚠️ Опасный пример (`escape` в комментарии):
```java
public class DangerousEscape {
    public static void main(String[] args) {
        // \u000A System.out.println("Выполнится!");
    }
}
```

Здесь `\u000A` интерпретируется как перевод строки до токенизации, и фактически закомментированный код превратится в выполняемый.
## Разделители строк (Line Terminators)
По спецификации `JLS (§3.4)` допустимы три варианта разделителей строк в исходных файлах:
- `\n` `(LF, U+000A)`
- `\r` `(CR, U+000D)`
- `\r\n` `(CRLF)`

Дополнительно: `Unicode escape \u000A` обрабатывается как настоящий перевод строки на раннем этапе, что может неожиданно изменить структуру кода.

Также допускается символ `form feed` (`\f`, `U+000C`) в исходных файлах, но он не считается разделителем строки.

✅ Пример
```java
public class LineTest {
    public static void main(String[] args) {
        System.out.println("OK"); // допустимы CR, LF или CRLF
    }
}
```

❌ Ошибочный пример
```java
public class Broken {
    public static void main(String[] args) { \u000A System.out.println("Hello");
    }
}
```
Здесь `\u000A` превращается в перенос строки, что может привести к синтаксическим ошибкам.
## Символы за пределами BMP
- Java использует `UTF-16` как внутреннее представление строк и символов.
- Символы из диапазона `U+10000` и выше (дополнительные плоскости `Unicode`) представляются как [[Cуррогатные пары|суррогатные пары]].
- В исходниках такие символы можно указывать напрямую, если кодировка файла поддерживает их.

✅ Пример
```java
public class Supplementary {
    public static void main(String[] args) {
        String emoji = "😀"; // U+1F600 (Grinning Face)
        System.out.println(emoji);
    }
}
```
## Тип `char` и `Unicode literals`

- Тип `char` в `Java` всегда 16-битный и хранит один кодовый элемент `UTF-16`, а не обязательно целый символ `Unicode`.
- Литералы `'\uXXXX'` всегда обозначают ровно один `char`.
- Символы за пределами BMP занимают два `char`, поэтому их нельзя указать в одиночном `char`-литерале.

❌ Ошибочный пример
```java
public class InvalidChar {
    public static void main(String[] args) {
        // попытка указать суррогатную пару в char
        char c = '\uD83D\uDE00'; // ошибка компиляции
    }
}
```

✅ Правильный вариант
```java
public class ValidString {
    public static void main(String[] args) {
        String emoji = "\uD83D\uDE00"; // 😀 в виде суррогатной пары
        System.out.println(emoji);
    }
}
```
## Версии Unicode и Java
Поддержка `Unicode` в `Java` зависит от версии `JDK`:
- Каждая версия JDK соответствует определённой версии `Unicode Standard`.
- Это означает, что новые символы становятся доступны только после обновления JDK.

Примеры:
- JDK 8 → Unicode 6.2
- JDK 11 → Unicode 10.0
- JDK 17 → Unicode 13.0

Поэтому код, использующий недавно добавленные символы, может не компилироваться или отображаться некорректно в старых версиях `JDK`.
## BOM (Byte Order Mark)
`BOM (Byte Order Mark)` — это специальный маркер в начале текстового файла, указывающий кодировку и порядок байтов.

- В **UTF-8** BOM не обязателен, но может встречаться (`U+FEFF`).
- В **UTF-16** BOM необходим для указания порядка байтов (`Big Endian / Little Endian`).
- Компилятор `javac` умеет корректно обрабатывать BOM, но некоторые инструменты и редакторы могут создавать проблемы при его наличии.

❌ Возможная ошибка (UTF-8 с BOM)
```java
﻿public class BomIssue { // первый символ — скрытый BOM (U+FEFF)
    public static void main(String[] args) {
        System.out.println("BOM test");
    }
}
```
Здесь ﻿ (невидимый BOM) может вызвать ошибку `class BomIssue is public, should be declared in a file named BomIssue.java`.
# Выводы
- Исходные файлы Java должны быть сохранены в кодировке, совместимой с `Unicode` (рекомендуется `UTF-8`).
- `Unicode escapes` обрабатываются до токенизации и могут неожиданно изменить структуру кода.
- Разделители строк строго определены `JLS`: `CR`, `LF`, `CRLF` (а `\f` допустим, но не `line terminator`).
- Символы за пределами BMP поддерживаются через суррогатные пары (`UTF-16`).
- Тип `char` хранит 16-битный `unit`, а не символ `Unicode` в общем случае.
- Поддержка новых символов зависит от версии `Unicode`, принятой в JDK.
- BOM может быть причиной ошибок и должен использоваться осторожно.
