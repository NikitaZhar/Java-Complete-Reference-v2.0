2025-11-25 14:29
Tags: #char #toFormat 

Тип `char` — примитивный интегральный тип Java, представляющий **16-битное беззнаковое значение**, соответствующее **UTF-16 code unit**. Он не гарантирует представление одного символа Unicode: символы вне BMP (Basic Multilingual Plane) требуют двух `char`.

---

## 4.2.6.1 Официальное определение (JLS)

**Определение:**

- Диапазон: `\u0000` … `\uffff` (0 … 65535).
    
- Тип: integral (целочисленный).
    
- Семантика: представляет **UTF-16 code unit**, а не Unicode code point и не графему.
    

---

## 4.2.6.2 Диапазон и представление

### 4.2.6.2.1 Диапазон значений

```
0x0000 … 0xFFFF
0 … 65535
```

### 4.2.6.2.2 Представление в памяти

- размер: 16 бит
    
- знаковость: беззнаковый
    
- расширение при продвижении (`widening`): **zero-extension**
    

### 4.2.6.2.3 Схема

```
┌────────────── 16 bits ──────────────┐
|  c15  c14 … c1  c0                  |
└──────────────────────────────────────┘
```

### 4.2.6.2.4 Таблица

|Свойство|Значение|
|---|---|
|Размер|2 байта|
|Тип|integral|
|Семантика|UTF-16 code unit|
|Диапазон|0–65535|

---

## 4.2.6.3 Литералы `char` (JLS §3.10.4)

### 4.2.6.3.1 Допустимые формы

- Символьные литералы: `'A'`, `'%'`
    
- Escape-последовательности: `'\n'`, `'\t'`, `'\''`, `'\\'`
    
- Unicode escape: `'\uXXXX'`
    
- Целочисленные литералы 0–65535
    

### 4.2.6.3.2 Корректные примеры

```java
char c1 = 'A';
char c2 = '\u03A9';   // Ω
char c3 = '\n';
char c4 = 90;         // 'Z'
char c5 = 65535;
```

### 4.2.6.3.3 Ошибочные примеры

```java
char e1 = 'AB';        // слишком много символов
char e2 = -1;          // вне диапазона
char e3 = '\u110000';  // недопустимый Unicode escape
```

---

## 4.2.6.4 Приведения и продвижение (JLS §5.1, §5.6)

### 4.2.6.4.1 Binary Numeric Promotion

При арифметических операциях:

- `char` всегда продвигается в `int`.
    

```java
char c = 'A'; // 65
int x = c + 5; // 70
```

### 4.2.6.4.2 Приведение обратно

```java
char r = (char)(c + 1); // 'B'
```

### 4.2.6.4.3 Ошибки

```java
char x = c + 1; // ошибка: результат — int, нужно приведение
```

---

## 4.2.6.5 Narrowing Conversion и Constant Variables (JLS §5.1.3)

### 4.2.6.5.1 Правило

Допустимо присвоение `char`:

- целочисленного литерала 0–65535;
    
- `final int`, который является compile-time constant и лежит в диапазоне.
    

### 4.2.6.5.2 Примеры: корректно

```java
char a = 100;

final int x = 200;
char b = x; // OK — x константа
```

### 4.2.6.5.3 Пример: ошибка

```java
int y = 200;
char c = y; // ошибка: y — не константа
```

---

## 4.2.6.6 Операции с `char`

### 4.2.6.6.1 Сравнение

```java
char a = 'x';
char b = 'y';
boolean r = a < b; // true
```

### 4.2.6.6.2 Арифметика

```java
char c = 'A';
char next = (char)(c + 1); // 'B'
```

### 4.2.6.6.3 Конкатенация

```java
char c = 'Z';
String s = "Letter: " + c;
```

### 4.2.6.6.4 Таблица операций

|Операция|Результат|
|---|---|
|`char + char`|`int`|
|`char + int`|`int`|
|`char == char`|сравнение значений|
|`"string" + char`|String|

---

## 4.2.6.7 UTF-16, суррогатные пары, Unicode code points

### 4.2.6.7.1 Принцип

**`char` не равен Unicode code point.**  
Символы `> U+FFFF` требуют **двух char**.

### 4.2.6.7.2 Пример

```java
String s = "\uD83D\uDE00"; // 😀
s.length();           // 2
int cp = s.codePointAt(0); // 0x1F600
```

### 4.2.6.7.3 Ошибка

```java
String s2 = "\uD83D"; // одиночный суррогат → некорректная строка
```

### 4.2.6.7.4 Подводные камни

- `length()` → количество code units, а не символов
    
- `charAt()` может вернуть «половину символа»
    
- операции над `char` не учитывают нормализацию Unicode
    

---

## 4.2.6.8 Unicode Escape Preprocessing (JLS §3.3)

### 4.2.6.8.1 Правило

Unicode escape `\uXXXX` обрабатываются **до** токенизации.

### 4.2.6.8.2 Пример: приводит к ошибке

```java
String s = "\u0022";
```

`\u0022` → `"`, компилятор увидит:

```
String s = """; // ошибка
```

### 4.2.6.8.3 Escape в комментариях

```java
// \u000A int x = 10;
```

`\u000A` — перевод строки, код становится:

```java
//
// int x = 10;
```

### 4.2.6.8.4 Опасность

Unicode escape может **менять структуру программы**, даже в комментариях.

---

## 4.2.6.9 Сравнение `char` с byte/short

### 4.2.6.9.1 Пример

```java
byte b = -1;
char c = 65535;

System.out.println(b == c); // true (!)
```

### 4.2.6.9.2 Причина

- `b → int = -1`
    
- `c → int = 65535`  
    Оба дают битовый паттерн `0xFFFF`.
    

---

## 4.2.6.10 Автоупаковка и `Character`

### 4.2.6.10.1 Корректные примеры

```java
Character ch = 'X';
boolean letter = Character.isLetter(ch);
int num = Character.getNumericValue(ch);
```

### 4.2.6.10.2 Ошибка

```java
Character c = null;
char x = c; // NullPointerException
```

---

## 4.2.6.11 Типичные ошибки и подводные камни

- «char = символ Unicode» — неверно
    
- некорректная работа с суррогатными парами
    
- значение вне диапазона при присвоении
    
- NPE при auto-unboxing
    
- ошибки narrowing conversion
    
- проблемы из-за Unicode preprocessing
    

---

## 4.2.6.12 Практические рекомендации

- Используйте `char` как UTF-16 code unit.
    
- Для логических Unicode-символов используйте `int` (code point).
    
- Для обработки текста используйте:  
    `String.codePoints()`, `Character.toChars()`, `Character.isSupplementaryCodePoint()`.
    
- Проверяйте диапазон перед приведением `int → char`.
    
- `String.length()` не отражает количество символов.
    

---

## 4.2.6.13 Мини-схема

```
4.2.6 char
    ├─ integral primitive
    ├─ 16-bit unsigned
    ├─ UTF-16 unit
    ├─ promotions → int
    ├─ narrowing conversion
    ├─ surrogate pairs
    ├─ unicode preprocessing
    └─ wrapper: Character
```

---

# 4.2.6.14 Unicode: графемные кластеры (UAX #29)

### 4.2.6.14.1 Принцип

**Графемный кластер ≠ code point ≠ char.**

Один визуальный символ может состоять из:

- одного `char`,
    
- двух `char` (суррогатная пара),
    
- нескольких code points (комбинируемые символы),
    
- нескольких emoji-компонентов (ZWJ-sequence).
    

### 4.2.6.14.2 Примеры

```
// графема "é"
"é" = U+00E9          // 1 char
"e\u0301"             // 2 char

// emoji flag 🇩🇪
"\uD83C\uDDE9\uD83C\uDDEA"  // 4 char
```

### 4.2.6.14.3 Вывод

Java **не предоставляет** API для графемных кластеров.  
Используйте ICU4J или реализацию Unicode Text Segmentation (UAX #29).

---

# 4.2.6.15 Производительность JVM и `char`

### 4.2.6.15.1 Основные факты

- В HotSpot операции `char` и `int` одинаковы по скорости (так как `char` продвигается в `int`).
    
- В Java 9+ `String` хранится в `byte[]` (Compact Strings), поэтому работа с `char[]` чаще _медленнее_.
    
- `char` не подходит как универсальный контейнер текстовых данных.
    

### 4.2.6.15.2 Практический вывод

Для высокопроизводительных систем:

- используйте `byte[]` + CharsetDecoder,
    
- избегайте хранения больших буферов в `char[]`.
    

---

# 4.2.6.16 Unicode и безопасность

### 4.2.6.16.1 Homoglyph attacks

Символы, которые выглядят одинаково:

- `'a'` (Latin) → U+0061
    
- `'а'` (Cyrillic) → U+0430
    

Используются в:

- фишинге,
    
- подмене пользовательских идентификаторов,
    
- регистрации почти одинаковых имён.
    

### 4.2.6.16.2 Комбинируемые символы

```
é = U+00E9  
é = U+0065 + U+0301
```

Строки выглядят одинаково, но различаются побитово.

### 4.2.6.16.3 Unicode в идентификаторах Java

Java разрешает Unicode-имена классов и переменных:

```java
class Рassword {} // Р — кириллическая, не латинская P
```

### 4.2.6.16.4 Вывод

При работе с пользовательским вводом:

- используйте нормализацию:  
    `Normalizer.normalize(str, Normalizer.Form.NFC)`
    
- проверяйте строки на допустимые диапазоны Unicode
    
- запрещайте смешивание алфавитов, если это важно для безопасности
    

---

Если хочешь, я могу собрать аналогичный расширенный раздел **4.2.7 `boolean`**, **4.2.5 `int`** или любой другой элемент JLS.