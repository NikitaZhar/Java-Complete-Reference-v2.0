---
created: 2025-10-07
status: done
part: "Part I"
chapter: 2
tags: [topic/grammars, src/jls]
---

Грамматика — это формальный аппарат, которым спецификация задаёт язык Java: какие последовательности символов образуют токены и какие последовательности токенов образуют корректную по форме программу. Java описывается двумя грамматиками — лексической и синтаксической — и обе записаны в единой нотации JLS. Эта глава учит читать эту нотацию и понимать, что именно грамматика задаёт, а что оставляет на следующий слой проверок. Это не каталог токенов (он в главе 3) и не свод семантических правил (каждое живёт в своей главе) — здесь только сам аппарат.

## Why Grammars Matter for Developers

Практическая ценность не в том, чтобы писать парсеры, а в умении читать компилятор. Ошибки `javac` делятся на два класса: одни нарушают грамматику (parsing error — «не удаётся разобрать»), другие проходят разбор, но нарушают семантику (`variable might not have been initialized`, несовместимость типов). Понимание, где проходит эта граница между формой и смыслом, экономит время при диагностике (карта, где искать каждую семантическую ошибку, — в 2.4). Кроме того, на грамматике держатся IDE, линтеры и статические анализаторы, а знание нотации JLS позволяет читать официальные правила напрямую и разбираться в тонких случаях с токенами и приоритетами.

## Grammars in the Compilation Pipeline

Грамматики работают на первых двух стадиях компиляции; дальше подключается семантика.

```
Исходный текст (Unicode)
   ↓ обработка \uXXXX, нормализация переводов строк
Поток символов
   ↓ лексическая грамматика (цель Input): выделение токенов
Токены
   ↓ синтаксическая грамматика (цель CompilationUnit): разбор
Дерево разбора
   ↓ семантические проверки: имена, типы, DA, исключения, доступ …
Корректная программа
   ↓
Генерация байткода
```

Эту схему держим только здесь; в подразделах она не повторяется.

## Chapter Map

## [[2.1 The Grammar Notation]]
Как читать нотацию JLS: терминалы, нетерминалы, продукции, `[x]`, `{x}`, `one of`, `opt`.

## [[2.2 The Lexical Grammar]]
Формализм лексики: цель `Input`, трёхэтапная трансляция, правило longest-match. Каталог токенов — глава 3.

## [[2.3 The Syntactic Grammar]]
Контекстно-свободная грамматика: цель `CompilationUnit`, токены → программа → дерево разбора.

## [[2.4 Grammars and Semantic Constraints]]
Граница возможностей грамматики и карта, где живёт каждое семантическое правило.

## [[2.5 Common Mistakes]]
Ошибки чтения грамматики и синтаксиса.
