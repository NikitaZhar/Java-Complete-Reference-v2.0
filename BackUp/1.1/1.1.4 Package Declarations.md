2025-09-25 11:01
Tags: #

## Кратко
`package` — первая (после комментариев и аннотаций пакета) строка в исходном файле, которая однозначно указывает, к какому пакету принадлежит компиляционная единица (файл). Имя должно быть полностью квалифицированным; файловая структура обязана соответствовать имени пакета.

Допускается **unnamed package** (неименованный пакет) — только для мелких/временных программ; его элементы недоступны из именованных пакетов.

Подпакеты — это **отдельные** пакеты; доступ по package-private между ними не распространяется.

Для документации и аннотирования пакета применяется `package-info.java`.

📎 См. также:

- [1.1.5 Import Declarations]
    
- [1.2.1 Access Modifiers]
    
- [1.4.2 Modules]
## Синтаксис
По [Java Language Specification §7.4](https://docs.oracle.com/javase/specs/jls/se21/html/jls-7.html#jls-7.4):
```
PackageDeclaration:
    {PackageModifier} package Identifier {. Identifier} ;
```
- `PackageModifier` — модификаторы пакета (обычно аннотации).
- `Identifier` — последовательность идентификаторов, разделённых точками.

Пример:
```java
package com.example.myapp.service;
```
## Порядок и правила
- В одном файле допустимо только **одно** объявление `package`.
- Оно должно находиться **перед любыми объявлениями типов** и **после комментариев или аннотаций пакета**.
- После `package` идут только `import` и объявления типов.
- Аннотации пакета записываются так:
```java
@Deprecated
package com.example.util;
```
## Идентификаторы и ключевые слова
- Части имени пакета должны быть **валидными идентификаторами Java**.
- Нельзя использовать ключевые слова (`class`, `module`, `open`, и т.д.).
- Unicode допустим, но рекомендуется использовать только латиницу и строчные буквы.
## Именование пакетов и соглашения по стилю
- Все буквы **строчные**.
- Не использовать подчёркивания и дефисы.
- Начинать с обратного домена организации: `org.example`, `com.company`.
- Избегать излишней глубины иерархии (более 3–4 уровней).

Пример:
```java
package org.openjdk.tools.compiler;
```
### Обязательные правила
- В одном файле — одно объявление `package`.
- Объявление `package` должно полностью соответствовать структуре каталогов исходников.
- Все файлы **внутри одного каталога пакета** должны начинаться с одинакового объявления `package`.
- В проекте запрещено смешение классов в именованном пакете и классов без пакета (unnamed) в одном модуле/артефакте.
### Стиль и конвенции (Oracle) 
- Строчные имена, иерархия по смыслу: `com.company.product.module`.
- Избегать очень длинных имён пакетов — глубина обычно не более 3–4 сегментов.
- Не использовать транслит и не-ASCII символы; избегать аббревиатур без крайней необходимости.
- Для открытых библиотек — документировать пакеты (`package-info.java`) и придерживаться стабильности имен.
## Unnamed Package (неименованный пакет)
- Файл без объявления `package` автоматически принадлежит `unnamed package`.
- Такой код нельзя импортировать из именованных пакетов.
- Используется исключительно для **малых, тестовых и временных программ**.

Пример:
```java
// Файл QuickTest.java
public class QuickTest {
    public static void main(String[] args) {
        System.out.println("Hello from unnamed package!");
    }
}
```
## Вложенные пакеты
- Подпакеты независимы:
    - `com.example` и `com.example.util` не имеют привилегированного доступа друг к другу.
- Видимость `package-private` ограничена рамками **одного пакета**.
## Взаимодействие пакетов с модификаторами доступа

|Модификатор|Доступ внутри пакета|Доступ за пределами пакета|
|---|---|---|
|**public**|доступен|доступен|
|**protected**|доступен|доступен наследникам в других пакетах|
|**(package-private)**|доступен|❌|
|**private**|только внутри класса|❌|

📎 См. также: [1.2.1 Access Modifiers]
## package-info.java
Файл `package-info.java` используется для:
- JavaDoc документации пакета,
- применения аннотаций,
- общих комментариев.

Пример:
```java
/**
 * Provides utilities for working with dates.
 * @since 1.0
 */
@Deprecated
package com.example.util.date;
```
### Генерация JavaDoc
```bash
javadoc -d doc-out -sourcepath src -subpackages com.example
```
## Использование `import`
Чтобы обращаться к классам из других пакетов, применяется `import`.
```java
import java.util.List;           // Импорт одного класса
import java.util.*;              // Импорт всех классов пакета
import static java.lang.Math.PI; // Статический импорт константы
import static java.lang.Math.*;  // Статический импорт всех членов
```
⚠️ `import package.*` **не затрагивает подпакеты**.

📎 См. также: [1.1.5 Import Declarations]
### Варианты _(восстановлено из v3)_
- Точечный импорт: `import a.b.C;` — явный, предпочтителен для читаемости.
- Пакетный импорт: `import a.b.*;` — подключает все **типы** пакета `a.b`, но **не** его подпакеты.
- Статический импорт: `import static a.b.C.MEMBER;` или `import static a.b.C.*;` — для констант и утилитных методов.
- Полные имена без `import`: допустимо, но ухудшает читаемость (`java.util.List l = new java.util.ArrayList<>()`).
## Организация файловой структуры
```
src/
 └── com/
     └── example/
         └── myapp/
             ├── Main.java
             └── utils/
                 └── Helper.java
```
### Замечания для сборки проектов
- **Maven**: `src/main/java`, `src/test/java`
- **Gradle**: те же пути по умолчанию
- При компиляции вручную использовать `javac -d build ...`
## Практики организации пакетов во фреймворках
### Spring Boot
- Главный класс (`@SpringBootApplication`) — в корневом пакете.
- Структура:
```
com.example.demo
├── controller
├── service
├── repository
├── model
└── config
```
- При необходимости менять корневой пакет — использовать `scanBasePackages`.
### Jakarta EE
- `api` — REST API
- `ejb` — бизнес-логика
- `persistence` — JPA сущности
### Jakarta EE (ранее Java EE) 
- Исторический контекст: переименование Java EE → Jakarta EE; пространства имён в артефактах сместились в `jakarta.*`.
- Организация пакетов в классических `EAR/WAR`:
    - `project-web` (WAR): `com.company.project.api`, `com.company.project.web`.
    - `project-ejb` (JAR): `com.company.project.ejb`, бизнес-слой.
    - `project-common` (JAR): модели/DTO, общие утилиты.
- В больших системах важно избегать `split packages` между модулями EAR.  
## Best Practices
- Всегда использовать **именованные пакеты**.
- Придерживаться конвенции `com.company.project`.
- Для публичных API — `package-info.java`.
- Избегать `*` в `import`.
- Не создавать слишком глубокую иерархию.
## Ошибки и подводные камни
1. Несоответствие структуры каталогов имени пакета
2. Дублирование классов в разных пакетах
3. Избыточная вложенность пакетов
4. Использование `unnamed package` в больших проектах
5. Злоупотребление `wildcard import`
6. Неверное понимание package-private доступа
7. `Split packages` в JPMS
8. `Automatic modules` с нестабильными именами
9. Отсутствие `opens` при работе с рефлексией (Spring, JPA)
## Named vs Unnamed Package

| Характеристика | Named package             | Unnamed package           |
| -------------- | ------------------------- | ------------------------- |
| Импорт         | Можно импортировать       | Нельзя импортировать      |
| Применение     | Производственный код      | Временный/учебный код     |
| Документация   | Через `package-info.java` | Нет                       |
| Совместимость  | Совместим с модулями      | Не используется в модулях |

## Import vs Static Import

| Импорт            | Что даёт                        | Пример                               |
| ----------------- | ------------------------------- | ------------------------------------ |
| `import`          | Подключение классов/интерфейсов | `import java.util.List;`             |
| `import *`        | Все классы пакета               | `import java.util.*;`                |
| `static import`   | Подключение статических членов  | `import static java.lang.Math.sqrt;` |
| `static import *` | Все статические члены           | `import static java.lang.Math.*;`    |

## Мини-кейс: «Падает сборка из-за несоответствия структуры каталогов имени пакета»
### Ситуация
Вы создаёте два класса — `Main` и `Helper`. В `Main` указываете пакет `com.example.app`, а файлы лежат не в соответствующих каталогах:
```
project/
└── src/
    ├── Main.java              // ❌ лежит не в com/example/app/
    └── utils/
        └── Helper.java        // ❌ лежит не в com/example/app/utils/
```
Содержимое `Main.java`
```java
package com.example.app; // заявлен пакет com.example.app

import com.example.app.utils.Helper;

public class Main {
    public static void main(String[] args) {
        Helper.printMessage();
    }
}
```
Содержимое `Helper.java`
```java
package com.example.app.utils; // заявлен пакет com.example.app.utils

public class Helper {
    public static void printMessage() {
        System.out.println("Hello from Helper!");
    }
}
```
### Симптомы
- При компиляции через `javac` из корня `src` возможны ошибки:
    - `error: cannot find symbol: class Helper`
    - `error: class Main is public, should be declared in a file named Main.java`
- При запуске — `Exception in thread "main" java.lang.NoClassDefFoundError: com/example/app/Main`.

Причина: **физический путь файлов не совпадает с объявленными пакетами**. Компилятор и ClassLoader ищут классы по директориям, соответствующим имени пакета.
### Правильная структура
```
project/
└── src/
    └── com/
        └── example/
            └── app/
                ├── Main.java
                └── utils/
                    └── Helper.java
```
**`com/example/app/Main.java`**:
```java
package com.example.app;

import com.example.app.utils.Helper;

public class Main {
    public static void main(String[] args) {
        Helper.printMessage();
    }
}
```
**`com/example/app/utils/Helper.java`**:
```java
package com.example.app.utils;

public class Helper {
    public static void printMessage() {
        System.out.println("Hello from Helper!");
    }
}
```
### Правильная структура и сборка
- Структура директорий **должна соответствовать** имени пакета.
- Источник компилируем с указанием директории вывода классов `-d`.
### Скомпилируем в папку build
```bash
cd project/src
javac -d ../build com/example/app/Main.java com/example/app/utils/Helper.java
```
### Запуск
```bash
cd ../build
java com.example.app.Main
```
### => Hello from Helper! 
Вывод программы:
```
Hello from Helper!
```
### Вариация ошибки с unnamed package 
```
project/
└── src/
    ├── QuickTest.java           // ❌ unnamed package
    └── com/
        └── example/
            └── app/
                └── Utils.java   // ✔ named package com.example.app
```
**`QuickTest.java`** (без `package`):
```java
// unnamed package
import com.example.app.Utils; // Попытка импортировать именованный пакет в файл без package

public class QuickTest {
    public static void main(String[] args) {
        Utils.hello();
    }
}
```
**`com/example/app/Utils.java`**:
```java
package com.example.app;

public class Utils {
    public static void hello() {
        System.out.println("Hello!");
    }
}
```
Смешивание unnamed и named пакетов вызывает проблемы поддержки и интеграции: код из `unnamed package` нельзя импортировать в именованные пакеты. Рекомендация — **всегда** использовать именованные пакеты во всём проекте.
### Чек-лист профилактики 
1. Имя пакета ⇄ путь к файлу: `a.b.c.Class` ⇒ `a/b/c/Class.java`.
2. Одна компиляционная единица — одно объявление `package` и оно **первое** в файле.
3. Не смешивать unnamed и named пакеты в проекте.    
4. Компилировать с `-d` в отдельный `build/` или `out/` каталог.
5. При конфликте имён из разных пакетов — использовать **полностью квалифицированные имена**.
## Итог
Пакеты — базовый инструмент организации кода и управления доступом. Они тесно связаны с системой модулей (JPMS), обеспечивают читаемость, масштабируемость и контроль доступа.
📎 См. также:
- [1.1.5 Import Declarations]
- [1.2.1 Access Modifiers]
- [1.4.2 Modules]

