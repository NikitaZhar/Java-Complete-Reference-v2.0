2025-10-27 15:33
Tags: #byte #short #int #long #char #toFormat 


## **4.2.1.1 Диапазоны и представление**

### **Фиксированные размеры и переносимость**

Все целочисленные типы Java имеют фиксированную длину и диапазон значений, определённый спецификацией, независимо от платформы и архитектуры процессора.

|Тип|Размер (бит)|Диапазон значений|Формат|
|---|---|---|---|
|`byte`|8|−128 ... 127|signed two’s complement|
|`short`|16|−32 768 ... 32 767|signed two’s complement|
|`int`|32|−2^31 ... 2^31−1|signed two’s complement|
|`long`|64|−2^63 ... 2^63−1|signed two’s complement|
|`char`|16|0 ... 65 535|**unsigned** (UTF-16 code unit)|

> Тип `char` — **единственный беззнаковый целочисленный тип** в Java, хотя чаще воспринимается как символьный.

### Представление отрицательных чисел (Two’s Complement)
Все целочисленные типы Java, кроме `char`, используют представлениe **two’s complement** (дополнительный код) для хранения отрицательных чисел.

**Правило формирования отрицательного числа:**

```
two’s complement(x) = ~x + 1
```

**Пример:**  
Представление `-1` в типе `byte` (8 бит):

|Операция|Биты|
|---|---|
|Положительное значение `1`|`0000 0001`|
|Инверсия|`1111 1110`|
|+1 (результат)|`1111 1111`|

**Вывод:** `byte b = -1;` хранится как `1111 1111`.

```java
byte b = -1;
int unsignedValue = b & 0xFF;
System.out.println(unsignedValue); // 255
```

> 🔎 **Инженерный вывод:** отрицательные значения — это не “минус число”, а битовый паттерн. При арифметике и побитовых операциях работает именно битовое представление.

---

## **4.2.1.2 Арифметика и переполнение**

### **Арифметика выполняется на уровне CPU**

- Знаковые типы (`byte`, `short`, `int`, `long`) используют формат two’s complement.
    
- Арифметические операции выполняются напрямую машиной без проверок выхода за диапазон.
    

### **Переполнение**

- Не вызывает исключения.
    
- Результат вычисляется по модулю `2^N`, где N — количество бит типа.
    

```java
int x = 2_000_000_000;
int y = x + x; // отрицательное число
```

> **Это silent bug** — ошибка, которая не бросает исключение и приводит к неверной логике.

### **Инженерные последствия переполнения**

- Java **не генерирует исключение** при переполнении целочисленного типа.
    
- Результат вычисляется **по модулю 2ⁿ**.
    
- Это приводит к **тихим логическим ошибкам**.
    

```java
int x = Integer.MAX_VALUE; // 2147483647
int y = x + 1;
System.out.println(y); // -2147483648
```

> ✅ Для контроля переполнения используйте:

```java
int result = Math.addExact(x, y); // выбросит ArithmeticException
```

> ⚠ **Никогда не предполагайте, что переполнение бросит исключение** — это источник багов в продакшене.

---

## **4.2.1.3 Numeric Promotion (числовое продвижение)**

### **Правила продвижения:**

1. `byte`, `short` и `char` продвигаются к `int` перед любой арифметической операцией.
    
2. Если один из операндов — `long`, весь результат продвигается в `long`.
    
3. Плавающая точка всегда доминирует (см. 4.2.11).
    

```java
byte a = 5;
byte b = 10;
byte c = a + b; // ошибка: (a+b) -> int
byte c = (byte) (a + b); // требуется приведение
```

Таблица продвижения типов**

|Операнд A|Операнд B|Результирующий тип|
|---|---|---|
|byte|byte|int|
|byte|short|int|
|short|char|int|
|int|long|long|
|long|float|float|
|float|double|double|

> 📌 Продвижение применяется ко **всем арифметическим и побитовым операциям**.

```java
byte a = 1;
byte b = 2;
int c = a + b; // результат int
```



---

## **4.2.1.4 Битовые операции и сдвиги**

|Оператор|Описание|Применим к|
|---|---|---|
|`&`, `|`,` ^`,` ~`|Побитовое И, ИЛИ, XOR, NOT|
|`<<`|Сдвиг влево (арифметический)|сохраняет знак|
|`>>`|Сдвиг вправо (арифметический)|сохраняет знак|
|`>>>`|Сдвиг вправо (логический)|заполняет нулями (важно при unsigned-обработке)|

```java
byte b = -1;
int x = b >>> 1; // 2147483647
```

> Операнды меньше `int` сначала продвигаются до `int`, затем применяется операция.

Различие `>>` и `>>>`**

|Оператор|Заполняет старший бит|Поддерживает знак|Пример результата|
|---|---|---|---|
|`>>`|копией знакового бита|да (арифметический сдвиг)|сохраняет знак|
|`>>>`|нулём|нет (логический сдвиг)|используется для unsigned|

```java
int x = -8; // 11111111 11111111 11111111 11111000
System.out.println(x >> 2);  // -2  (знак сохраняется)
System.out.println(x >>> 2); // 1073741822 (дополняется нулями)
```

> 📝 `char` всегда unsigned, поэтому `>>` и `>>>` дают одинаковый результат.


---

## **4.2.1.5 Ошибки и edge-cases**

|Ошибка|Причина|Последствие|
|---|---|---|
|Переполнение `short` / `byte`|Циклическое поведение|Бесконечные циклы, логические баги|
|Использование `char` как числа|UTF-16, переполнение|Переход 65535 → 0|
|Ожидание исключения при переполнении|Неверное понимание two’s complement|Тихие ошибки|
|Сравнение `char` как символов|Не соответствует Unicode code point|Неверная обработка текста|
|Promotion `byte/short` к `int`|Незаметное расширение типа|Лишние касты или ошибки|


|Ошибка|Причина|Последствие|
|---|---|---|
|Использование `char` как целого типа|Беззнаковый диапазон, UTF-16|Переполнение → 0|
|Ожидание исключения при переполнении|JVM использует two’s complement|Тихая смена знака|
|Несоответствие при битовых сдвигах|`>>` сохраняет знак, `>>>` — нет|Ошибки при обработке флагов|
|Использование отрицательных `byte` как unsigned|Биты интерпретируются как signed|Неверная логика протоколов|


---

## **4.2.1.6 Best Practices**

✅ Использовать `int` по умолчанию  
✅ Использовать `long` для времени, ID, больших диапазонов  
✅ Использовать `byte[]` или `short[]` только при **массовом хранении бинарных данных**  
✅ Всегда учитывать переполнение:

```java
Math.addExact(), Math.subtractExact()
```

✅ Для unsigned байтов применять маску:

```java
int ub = b & 0xFF;
```


```java
int unsignedByte = Byte.toUnsignedInt(b);       // Java 8+
long unsignedInt = Integer.toUnsignedLong(i);   // безопасное преобразование
```

> Рекомендуется использовать unsigned-преобразования при работе с бинарными протоколами и файлами.

---

## **4.2.1.7 Anti-patterns**

|Антипаттерн|Пояснение|Результат|
|---|---|---|
|Использование `short`/`byte` в циклах|Не ускоряет, создаёт проблемы|Переполнение, promotion|
|Использование `char` как integer|Неверно для Unicode|Ошибки символов|
|int для timestamp|Переполнение 2038 года|Сбой времени|
|Сравнение wrapper-типов через `==`|Сравнение ссылок, а не значений|Непредсказуемое поведение|

---

## **4.2.1.8 Перекрёстные ссылки**

- Приведение типов: [[4.2.11 Promotions and Conversions]]
    
- Боксинг: [[4.2.12 Boxing and Unboxing]]
    
- Модель памяти JVM: [[4.2.18 Performance and JVM Considerations]]
    
- Символьные типы и Unicode: [[4.2.6 char (to Format)]]
    
- Плавающая точка: [[4.2.7 Floating-Point Types]]
    
